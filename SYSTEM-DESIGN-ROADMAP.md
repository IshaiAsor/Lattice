# Lattice — System Design & Development Roadmap

> **How to use this file:** every work session starts and ends here. Each item is the
> smallest independently testable step. Pick any unblocked item ("Needs" column empty or
> done) and say "let's do F2.3". After landing, its status gets updated so we can always
> resume exactly where we stopped.
>
> **Status legend:**
> `[ ]` not started · `[~]` in progress · `[R]` code done, awaiting your review ·
> `[S]` merged to `v2.2`, deployed to staging · `[x]` tested by you & accepted
>
> **Ground rules (decided 2026-06-12, amended 2026-07-02):**
>
> - Reference code lives on `v2.2-snapshot` (iot-smart-home `01177fb`, gitops `c2d5af2`) — starting point, never copied blindly; every piece is re-reviewed/rewritten with you.
> - Work merges to branch `v2.2`, deploys to **staging only**. `master`/`main` = live prod — untouched by default.
> - **2026-07-02:** the "never touch the prod overlay before F14" rule was explicitly relaxed for the backend-monolith deletion (user call: "prod doesn't matter right now") — `k8s/overlays/prod` now also routes `/api` → `api` and has an `api` deployment. This is a one-off exception for that change, not a blanket F14 waiver; **any future prod-overlay edit still needs an explicit ask first**. (Update: this gitops change was pushed to `main` on 2026-07-02 as commit `427fc51` and has since been applied to the live cluster — see Backend Teardown Tracker.)
> - Plan session before each feature group (F1, F2, …); small items inside a group can flow without a full re-plan.

**Current position: F1.5–F1.9 done; F2 (`api` service) done and now serving `/api` on both staging and prod ingress; the `backend/` monolith is fully deleted. F3.2/F3.3/F3.4/F3.6 verified + accepted (F3.3 had a real bug, fixed — see session log); F3.7 static code review done + a real bug fixed (F3.3-class dangling-pointer/NVS-leak bug), but stays `[~]` pending on-device BLE/NVS verification against staging hardware (Wokwi ruled out — unreliable twice). **F5 core audited 2026-07-03**: F5.1–F5.6/F5.8 were already built (on `master`, pre-dating this tracker) and confirmed wired to the new v2.2 services (api/device-gateway/socket-server, no leftover monolith routes) — all → `[x]`. F5.9 (admin users page) and F5.10 (user preferences page) don't exist yet; **moved to backlog**, not required for F5 close-out. F5.7 (backoffice CI/staging deploy) remains `[~]`, the only open F5 item. **Wider audit 2026-07-03 found F6.3, F7.1, F8.1, and F8.2 also already built and undocumented** (all → `[x]` now; see their rows for detail — F8.2 was misaudited as incomplete on a first pass, corrected after user testing disagreed; the coordinator runs enrich/command_exec/finalization in-process and only queues the actual ML infer stage). The one real gap still open in this area is **F9.1**'s fast-path/cache half (the CRUD half was absorbed into F6.3 when emergency merged into rules at F1.7). Next unblocked: F3.8 on-device OTA verification (blocked locally — no firmware artifacts in this dev environment; needs staging too); in parallel, F9.1's fast-path eval, F8.5 (pipeline CI+staging, now unblocked), and smaller unblocked items F6.4, F2.4a, F13.1. F3.9–F3.14 stay blocked until F3.7/F3.8 get real hardware time on staging. Gitops changes pushed 2026-07-02 (commit `427fc51`); ArgoCD pruned the old `web-app` deployment from prod. The only remnants left afterward were cosmetic naming (`web-app-ingress`/`web-app-tls`), renamed to `lattice-ingress`/`lattice-tls` on 2026-07-05.**

> **2026-07-03 CI/staging check:** all 13 GitHub Actions workflows are green on `master` HEAD (`029c4b3`). Kargo auto-promoted every staging Warehouse successfully **except `ota-staging`**, which errored ~2h ago pushing its commit to `oci-k3s-gitops` (`Could not resolve host: github.com` from inside the Kargo controller pod — looks like a transient outbound-DNS blip, not a config issue). Freight for `lattice-ota-manager:v2.0.177` (the OTA notification-publish change below) is stuck behind that error and hasn't reached staging yet — needs a manual promotion retry.

---

## Backend Teardown Tracker

**Done (2026-07-02): `backend/` monolith deleted entirely** (code, `Dockerfile.backend`,
`server-ci.yml`, VSCode debug configs/workspace entry). Its 3 remaining pieces were ported first:
Google OAuth account-linking (`/api/google/auth`, `/auth/login`, `/token`) was already fully
built in `google-home` (just unrouted) — added `/api/google` ingress prefix; device lifecycle
commands (reprovision/soft-reset/hard-reset/restart) and admin-catalog actions-list +
set-default-trait were added to `api`. `k8s/base/web-app` deleted; **both staging and prod
overlays** repointed `/api` → `api` service and `/api/google` → `google-home` (prod didn't have
an `api` deployment at all yet — added `../../base/api` + image pin + ingress + Kargo
`api-prod`-ready; prod `api-prod` promotion stays manual, matching every other `*-prod` stage).
Kargo `app-staging`/`app-prod` (formerly "server + migrations") now ships **migrations only**;
`lattice-server` dropped from the `lattice-app` Warehouse subscription. Backoffice repointed off
`environment.apiUrl` entirely (removed the field); 2 dead methods (`addDevice`,
`updateDeviceStatus`) and 2 unused fields deleted along the way. Gitops changes were pushed to
`main` (commit `427fc51`, 2026-07-02), and ArgoCD pruned the old `web-app` deployment from the
live prod cluster. The remaining `web-app-ingress`/`web-app-tls` naming (ingress resource name +
shared TLS secret used by ingress and EMQX) was cosmetic-only and renamed to
`lattice-ingress`/`lattice-tls` on 2026-07-05.

Strangler decomposition of the `backend/` monolith (`lattice-server`), one responsibility at a
time — history below, now complete.

| Responsibility (backend)                                           | Target service               | Status                                                         |
| ------------------------------------------------------------------ | ---------------------------- | -------------------------------------------------------------- |
| MQTT bridge (`mqtt.service`)                                       | `mqtt-service`               | 🟢 F3.4                                                        |
| Telemetry/status/OTA processing, socket-emit, sensor history       | `digest-service`             | ✅ F6.1                                                        |
| Provisioning `/provision` + `/refresh-token` + `/provision-token`  | `device-gateway`             | 🟢 F3.2                                                        |
| Device config (`device.configuration.*`)                           | `device-gateway`             | 🟢 F3.3                                                        |
| Camera intake (`camera.routes`, `ws.stream.service`)               | `device-gateway` → telemetry | 🟢 F3.6                                                        |
| Rules engine + scheduled-rules cron (`rules.engine.service`)       | automation worker            | 🟢 F6.2                                                        |
| Socket.IO **server** (`socket.service`, `socket.actions.service`)  | `socket-server`              | 🟢 F2.7                                                        |
| Auth / login / users                                               | `api`                        | 🟢 F2.2/2.3                                                    |
| Device & action mgmt (incl. lifecycle commands)                    | `api`                        | 🟢 F2.5/2.6                                                    |
| Admin catalog + google types/traits (incl. actions/default-trait)  | `api`                        | 🟢 F2.4                                                        |
| Google Home SYNC/QUERY/EXECUTE + HomeGraph + OAuth account-linking | `google-home`                | 🟢 F7                                                          |
| Sensor-history prune cron                                          | retention worker             | ⬜ F6.5 — never had a backend equivalent to retire; still open |
| Static UI serving                                                  | backoffice nginx             | ✅ F5.7                                                        |

VLM and Emergency rows removed: neither ever had a route mounted in `backend/index.ts` by the
time of deletion (VLM was already fully on ml-router/ml-executor; Emergency was never built on
the monolith) — nothing to strangle there, F8/F9/F11 remain open as net-new work, not migrations.

🟢 = new owner built and no backend equivalent remains (backend itself is now deleted).

---

## F1 — Foundation: monorepo + schema 2.2

The root everything else depends on. No deploy artifact; verified locally.

| #     | St  | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Test                                                                          | Needs                                                                                                                                                                                                                                                                                                                                                                                       |
| ----- | --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1.1  | [x] | Monorepo workspace skeleton: root `package.json` (workspaces), `tsconfig.base.json`. Old `backend/`/`backoffice/` keep building untouched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `npm install` at root; `backend` + `backoffice` still build                   | —                                                                                                                                                                                                                                                                                                                                                                                           |
| F1.2  | [x] | `packages/logger` — pino-style structured logger                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | builds; sample script prints structured log                                   | F1.1                                                                                                                                                                                                                                                                                                                                                                                        |
| F1.3  | [x] | `packages/otel` — tracing bootstrap (no-op when no collector)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | builds; service starts with and without `OTEL_EXPORTER_OTLP_ENDPOINT`         | F1.1                                                                                                                                                                                                                                                                                                                                                                                        |
| F1.4  | [x] | `packages/queue` — RabbitMQ helpers. **Use the approved queue design** (→ Appendix A): static routing keys (no userId), DLQ_ARGS, exact bindings, typed payloads                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | builds; publish/consume round-trip against local compose RabbitMQ             | F1.1                                                                                                                                                                                                                                                                                                                                                                                        |
| F1.5  | [x] | Schema 2.2 review — **part 1: identity & catalog** (User, DeviceModel, ActionDef, traits, ML models). Line-by-line session; you approve every model.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | review session; `prisma validate`                                             | —                                                                                                                                                                                                                                                                                                                                                                                           |
| F1.6  | [x] | Schema 2.2 review — **part 2: user device layer** (UserDevice, UserDeviceModel, UserAction, UserActionDef, groups, state)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | review session; `prisma validate`                                             | F1.5                                                                                                                                                                                                                                                                                                                                                                                        |
| F1.7  | [x] | Schema 2.2 review — **part 3: automation** (Rules, conditions, rule-actions, concrete Pipelines, stages, EmergencyRules)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | review session; `prisma validate`                                             | F1.6                                                                                                                                                                                                                                                                                                                                                                                        |
|       |     | **Rules**: `UserRule` timestamps → non-nullable; `UserRuleCondition` gained an optional `user_device_action_id` FK (threshold/device_status integrity; schedule/vlm null). RuleAction/Emergency unchanged. **ML unified**: `DeviceModelConfig` + `ModelAnalysisLog` (the F1.6 ML tables) **deleted** — a single-model analysis is now a 1-stage pipeline; `PipelineRun`/`PipelineRunStage` are the audit trail. **Concrete Pipeline tier (6 tables, user-owned, bound to `UserDeviceAction`)**: `Pipeline`, `PipelineSensor` (grouped read inputs + min/max range), `PipelineStage` (`kind` = infer/sensor_digest/command_exec; `infer`→`MlModel` FK, `command_exec`→execute action + value from preceding LLM structured output), `PipelineTrigger` (telemetry/schedule/manual — a camera frame is telemetry on an image action), `PipelineRun`, `PipelineRunStage`. **Abstract capability-based `PipelineBlueprint` + derive deferred to F10** (build the abstraction with its consumer; resolve capability portability FK-vs-semantic-key + snapshot-vs-live propagation there; apply same treatment to rules+emergency). Both schema files validate. No migration (that's F1.8).                                                                                                                                                                                             |                                                                               |                                                                                                                                                                                                                                                                                                                                                                                             |
| F1.8  | [x] | Init migration + `packages/prisma-client` package + generate flow                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `prisma migrate dev` on a fresh local DB; client imports from a sample script | F1.7                                                                                                                                                                                                                                                                                                                                                                                        |
|       |     | **Package + generate flow** (pre-existing): `packages/prisma-client` exports `db` (PrismaClient + `@prisma/adapter-pg` Pool from `DATABASE_URL`) and re-exports generated types; client derived from the root schema by the gitignored `prepare-prisma.mjs` (root = single source of truth; the packages copy is auto-derived — no need to hand-edit it). Prisma v7 (driver adapter, no datasource url). **Init consolidation (this session):** squashed all 20 old migrations → a single `0_init` generated via `prisma migrate diff --from-empty --to-schema` (since `migrate dev`/`reset` run the now-broken seed and this Prisma build lacks `--skip-seed`). Verified on a wiped local DB: `migrate deploy` applies it, `migrate status` clean, `migrate diff` DB↔schema empty. **No baselining anywhere** — prod data is expendable (reset→init→seed at F14). Regenerated `@lattice/prisma-client` against the new schema. **Reconciled 4 already-built services** that lagged the F1.5/F1.6 renames (`DeviceAction`→`DeviceCapability`, `action_id`→`capability_id`, `action`→`capability`, `group_name`→`group_id`, `ActionTypeTrait`→`DeviceCapabilityTrait`, dropped the separate `DeviceAction` template upsert in device-gateway's action-migration): digest-service, automation-worker, google-home, device-gateway — all typecheck clean. **gitops:** removed the ` |                                                                               | migrate resolve --applied 20260527170346_init_schema`drift hack from`migration-job.yaml`. Full schema review written to `prisma/SCHEMA.md` (mermaid ERD + per-table examples). **Note:** seeds (`seed.ts`/`seed-catalog.ts`) still target the pre-F1.5 columns → F1.9. **Connection quirk:** local Prisma needs `127.0.0.1`(not`localhost`, IPv6) + `DATABASE_URL`exported from root`.env`. |     |     |
| F1.9  | [R] | Seed script (reference catalog: device models, action defs, google types/traits — **generic naming only**)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | run seed; inspect via Adminer                                                 | F1.8                                                                                                                                                                                                                                                                                                                                                                                        |
| F1.10 | [ ] | Pin slot descriptions from firmware — currently hardcoded in the UI (`mat-hint` in device-config). Fix: (1) add `description VARCHAR(255)` to `device_capability_pins` (new migration via `migrate diff`); (2) firmware: add `description` to pin descriptor struct in `blueprint()` for each action class; (3) provisioning + `seed-catalog.ts`: store the description column; (4) `GET /capabilities` (api) + `GET /config` (device-gateway) already serialize all pin fields — include `description`; (5) UI reads `slot.description` from API response (already bound in template via `PinSlot.description`), no hardcoded strings remain.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | activate a capability → pin hint shows firmware-authored description          | F1.9, F3.7                                                                                                                                                                                                                                                                                                                                                                                  |
|       |     | **`seed.ts` was already schema-compatible** (google types/traits/users/mqtt_user unchanged). **Rewrote `seed-catalog.ts`** for the F1.5/F1.7 split: `device_capability_blueprints` no longer holds `configurable_pins`/`google_traits`/`google_action_type` inline — now writes the `google_type_id` FK + child tables `device_capability_pins` (key,label,mode) and `device_capability_traits`(→`google_device_traits`). Google types/traits **upserted by `value`** (catalog self-populates from firmware; keeps friendlier seeded names). Per-manifest transaction; pins/traits delete+reinsert so re-seed is exact. **Verified** vs local DB from the 4 committed manifests: 4 devices / 44 capabilities / 60 pins / 56 trait links, all Google FKs resolved; idempotent on re-run; labels all generic IoT (sensors/camera/fan/outlet/light dimmer), no farming terms. **Added `dotenv` to `prisma/package.json`** (the migrations image runs `npm install` on prisma/ only, so it must be a direct dep — the in-cluster catalog seed step was previously red). migration-job already runs `migrate deploy && prisma db seed && ts-node seed-catalog.ts` with manifests baked into the image.                                                                                                                                                                                |                                                                               |                                                                                                                                                                                                                                                                                                                                                                                             |

## F2 — `api` service (port 3000)

The user-facing API. Replaces the monolith's non-device routes.

| #     | St  | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Test                                                                              | Needs      |
| ----- | --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ---------- |
| F2.1  | [R] | Service skeleton: express app, `env.config.ts`, exception middleware, `/health`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `curl /health` → 200                                                              | F1.1       |
| F2.2  | [R] | Auth: Google sign-in (auth-code flow), JWT issue/refresh, auth middleware (`app_usage` purpose)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | login via curl/Postman with a real Google code; protected route 401/200           | F2.1, F1.8 |
| F2.3  | [R] | Users service + routes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | CRUD via curl as admin                                                            | F2.2       |
| F2.4  | [R] | Admin catalog: device models / action defs / google types+traits CRUD                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | CRUD via curl; seeded data visible                                                | F2.2       |
| F2.4a | [ ] | **Admin catalog enrichment:** admins can set `description` and `display_name` on `DeviceModel` and `DeviceCapability` rows. These are editorial overrides layered on top of the firmware-authored catalog — the seed populates structural fields; admin edits add human-readable context that survives re-seeds (separate nullable columns, not touched by the seed idempotent upsert). Schema: add `description VARCHAR(512)` + `display_name VARCHAR(128)` to both `device_models` and `device_capabilities` (new migration via `migrate diff`). API: `PATCH /api/admin/catalog/devices/:id` and `PATCH /api/admin/catalog/capabilities/:id` accept `{description?, display_name?}` alongside the existing Google curation fields. UI: admin catalog page with an inline edit for these fields on each device and capability card.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | edit a device/capability description → persists; re-seeding does not overwrite it | F2.4, F1.8 |
| F2.5  | [R] | Device mgmt: register device for user, list, rename, delete                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | CRUD via curl                                                                     | F2.4       |
| F2.6  | [R] | User actions + action groups (groups as entities)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | CRUD via curl                                                                     | F2.5       |
|       |     | **Built `services/api` (port 3000)** mirroring the device-gateway skeleton (logger/otel/jwt/prisma-client; no queue). **F2.2 auth** (user chose Google + credentials + register): `POST /api/auth/{login,google,register}` → bare JWT (monolith contract); `JwtService` mints `app_usage`/`app_usage_refresh`, claim keeps `id` (socket-server reads `decoded.id`); `requireAppToken` + `requireAdmin` middleware; auth rate-limiter. **F2.3 users**: `/api/users/me` + admin list/get/patch(role,type,name)/delete. **F2.4 catalog** (`/api/admin/catalog`, admin-only): Google action-types + device-traits **full CRUD** (reference data); Device catalog is build-published from firmware ([[project_catalog_build_publish]]) so it's **read + delete + capability Google-mapping curation** (`PATCH /capabilities/:id` sets `google_type_id` + replaces trait links), not hand-authored. **F2.5 device mgmt** (`/api/devices`): list (with `update_available`)/rename/delete, owner-scoped — registration stays in device-gateway provisioning ([[project_provisioning_redesign]]), so api does **not** create user_devices. **F2.6** (`/api/actions` + `/api/action-groups`): groups are now **entities** (`UserActionGroup`+`group_id` FK, replacing the old `group_name` string); actions = list (joins capability google type/traits + group)/rename/regroup/reorder/delete; action _creation_ stays in provisioning/device-config. All smoke-tested vs local DB (401/403/404/204 + happy paths; throwaway rows cleaned). **TS2742** fixed with explicit return annotations on catalog methods touching the `Json` trait field.                                                                                                                                                                                                                                                                                                       |                                                                                   |            |
| F2.7  | [x] | Socket.IO server: JWT handshake, room-per-user, `action_state_update` contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | socket client script connects & receives a test emit                              | F2.2       |
|       |     | **Shipped standalone as `services/socket-server` (port 3007)**, not folded into `api` — a socket relay needs none of api's machinery and only _verifies_ `app_usage` JWTs (login lands at F2.2). Holds the `@socket.io/redis-adapter` over Valkey, so digest's `@socket.io/redis-emitter` events (`action_state_update` — including camera frames keyed by actionId — and `device_status_change`) reach `user_{userId}` rooms with no server-side emit code. reads the app_usage user id from `decoded.id ?? decoded.userId` (monolith signs `id`); rooms match digest's `user_{id}`. Inbound `action_state_update {actionId,state}` → publishes `action.requested` (digest resolves + dispatches; see below). No DB. Infra: Dockerfile + `socket-server-ci.yml` (master+v2.2) + `k8s/base/socket-server` + staging overlay; **staging ingress `/socket.io` moved web-app → socket-server**. **Inbound command path (digest owns DB write + echo):** UI knows only the action id, and the device body must be `{value,duration}`, so socket-server publishes `action.requested {userId,actionId,value,duration}` → digest `action-requested.consumer` resolves actionId → `{user_device_id, mqtt_action_name, device.version}`, writes optimistic `current_state` + cache + echo (echo follows the DB write), then publishes `action.dispatch` for mqtt-service → device. (Superseded the earlier `q.action.dispatch.state` fan-out idea — it wrongly treated `command`==state.) **UI repointed:** `environment.socketUrl`; `device_status_change` field `state`→`online`. **Valkey ACL fix (affected digest's emitter too):** added `allchannels` to the user ACL in `compose.yaml` + `k8s/base/valkey` — pub/sub was `NOPERM` without it (used `allchannels`, not `&*`, because `&` is a shell control char that truncated the command). Verified: builds green + runtime (id-token connect, room delivery, inbound → `q.action.requested`). |                                                                                   |            |
| F2.8  | [R] | `backend/Dockerfile` + `lattice-api` CI workflow (triggers on `v2.2`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | image builds in CI; pushed to ghcr                                                | F2.1       |
| F2.9  | [R] | k8s base `api/` + staging overlay switch begins (api alongside old stack or replacing web-app in `iot-stg` — decided in plan session)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `/health` reachable on staging ingress                                            | F2.8       |
|       |     | **Infra**: `services/api/Dockerfile` (device-gateway pattern; **also copies `services/api/node_modules`** — express-rate-limit/google-auth-library nest there, not hoisted; see [[reference-service-dockerfile-nested-deps]]) — **verified: image builds locally + container serves `/health`**. `.github/workflows/api-ci.yml` (`lattice-api`, master+v2.2, multi-arch by-digest). `k8s/base/api` (Deployment+Service; env: DB_URL, JWT_SECRET, GOOGLE_SIGN_IN_*, ALLOWED_ORIGINS, OTel). Kargo `lattice-api` warehouse + `api-staging`(autoPromote)/`api-prod`(dormant) stages. **Staging switch — decided**: api gets its **own subdomain** `api.lattice-iot-stg.duckdns.org` (reachable + testable) while the monolith keeps serving `/api/*` on the main host; the path-prefix cutover (moving `/api/auth                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | users                                                                             | devices    | actions | ...` off web-app) happens as **F5** repoints the backoffice to the new endpoints/response shapes — avoids breaking the old deployed UI mid-rebuild. Prod overlay untouched (F14). kustomize staging renders; kargo manifests validate. |     |     |

## F3 — `device-gateway` (port 3004) + ESP32 firmware

Everything a device touches. The contract review (F3.3/F3.5) is where most of your v1 dislikes likely live — budget real time there.

| #    | St  | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Test                                                                                                      | Needs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F3.1 | [x] | Service skeleton + `/health` + device JWT middleware (`device_usage`/`provisioning` purposes)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | curl with/without token                                                                                   | F1.1, F1.8                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| F3.2 | [x] | Provisioning flow (server side): register → finalize → returns NVS payload (`device_id`, tokens, urls, actions w/ pins + telemetry interval). **BLE contract review against firmware.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | provisioning E2E with curl simulating the app/device                                                      | F3.1, F2.5                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
|      |     | **Ported into device-gateway** the device-facing parts from `backend/services/provisioning.service.ts` (already close to [[project-provisioning-redesign]]): `POST /provision` (device_provisioning → upsert devices + capability blueprints + user_devices by mac → permanent JWT) and `POST /refresh-token`. **`GET /provision-token` stays on the backend/api** (app-facing, UI mints it) — staging ingress routes that exact path to web-app; the token it signs is verified here on `/provision`. JWT claim shapes preserved byte-identical for EMQX (`device_usage` = `{userid,clientid}`); refresh token now carries `version`. URLs built from `env.baseUrl`. **Verified 2026-07-02** end-to-end via curl against the live compose stack: admin login → `GET /provision-token` (app_usage) → `POST /provision` (device_provisioning token) → permanent `mqttToken`+`refreshToken` returned, `user_device` row created (upsert-by-mac confirmed idempotent on repeat calls) → `POST /refresh-token` correctly exchanges the refresh token for a fresh pair. BLE contract review against firmware deferred to F3.7 (this task only covers the server side). Accepted by user.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |                                                                                                           |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| F3.3 | [x] | Device config endpoint (`GET /config`) + token refresh endpoint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | curl as device                                                                                            | F3.2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
|      |     | **Ported into device-gateway**: `GET /api/device/:version/configuration` (**device_usage only** — tightened from the old `device_usage OR app_usage`; the app_usage path was unused and broken in the old backend, and the UI reads config via the api management endpoints) → `{device_type, device_version, actions[]}`. Refresh-token endpoint landed under F3.2. device-gateway is now purely device-authenticated. **Verified 2026-07-02 — found and fixed a real bug**: `device-configuration.routes.ts` read `req.device?.deviceId`, but the device_usage JWT is minted with `{userid, clientid}` (EMQX-compatible field names per F3.2's note) — `deviceId` never existed on the payload, so every legitimately provisioned device got `400 Missing deviceId` from `GET /config`, 100% of the time. `camera.routes.ts` already had the correct pattern (`{userid, clientid}` cast); applied the same fix here (`req.device?.clientid`). Rebuilt + restarted device-gateway, re-tested with a freshly provisioned device token → `200 {device_type, device_version, actions:[]}`. Accepted by user.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |                                                                                                           |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| F3.4 | [x] | **`services/mqtt-service`** (port 3005) — standalone generic MQTT↔queue bridge: TopicRouter (handler registry pattern, extensible), device-status handler → `q.device.state.changed`, device-telemetry handler → `q.telemetry.arrived`, action-dispatch consumer (`q.action.dispatch` → MQTT command). No DB. `firmwareVersion?` added to `ActionDispatchPayload`. CI + k8s base + staging overlay wired. **Verified 2026-07-02** against the live compose stack + `device-sim` fleet: continuous telemetry flow observed end-to-end (device-sim → EMQX → mqtt-service → `q.telemetry.arrived` → digest state writes); manual `action.dispatch` publish (RabbitMQ mgmt API, no `mosquitto_pub` available) → mqtt-service → MQTT `command/outlet` topic → device-sim received + acked → ack routed back through mqtt-service → `q.action.result` → digest wrote authoritative state. All 4 static queues present with active consumers. Accepted by user.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | mosquitto_pub status/telemetry → RabbitMQ queues visible; publish `action.dispatch` → MQTT command logged | F1.4                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| F3.6 | [x] | Camera frame HTTP intake (`POST /camera/frame`) → queue/socket                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | curl a jpeg → event observed                                                                              | F3.4                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
|      |     | **Ported into device-gateway as a pure pass-through, rewired to telemetry** — both intake mechanisms: (a) HTTP `POST /api/camera/frame?action=<mqtt_action_name>` (device_usage, raw JPEG ≤2mb); (b) **WebSocket** `/ws/stream` + `/ws/capture?token=…&action=<mqtt_action_name>` (`ws.stream.service` port). **Contract changed (with firmware): the device sends its own `mqtt_action_name`** (same convention as MQTT telemetry topics), so the gateway just publishes `q.telemetry.arrived` (base64) — no DB lookup, no `implementation_type` knowledge, no cache. digest-service resolves + classifies as `image` kind and owns DB/`camera_frame` cache/socket; unknown actions DLQ there. Firmware updated: `HttpFrameService` (`?action=`), `LiveStreamService` (`&action=`), the 2 HTTP action classes, `DynamicDeviceActionsService` begin calls — `pio run -e esp32s3_cam-test` SUCCESS. Staging ingress routes `/ws` → device-gateway. **Verified 2026-07-02**: provisioned an `ESP32S3_CAM` device, POSTed a raw JPEG to `/api/camera/frame?action=test_cam` with its device token → `200`, confirmed on the digest-service side the frame arrived via `q.telemetry.arrived` and was correctly resolved-or-DLQ'd (DLQ'd here since `test_cam` wasn't an activated capability on the throwaway device — expected/correct behavior, not a bug). Pass-through plumbing confirmed intact end-to-end; WS path not separately exercised (same handler pattern as camera.routes.ts, already fixed alongside F3.3). Accepted by user.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |                                                                                                           |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| F3.7 | [~] | ESP32 firmware review: DeviceRegistrationService, RefreshToken model, NVS layout, provisioning BLE, mocks + wokwi sim                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `pio run -e esp32s3_mini-test`; wokwi boot; provision against local gateway                               | F3.2, F3.3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
|      |     | **Wokwi ruled out** (failed twice — BLE/WiFi emulation unreliable for this flow); real hardware testing deferred to staging. **Static code review done 2026-07-02** (build-verified, not hardware-run) covering `main.cpp` boot/provisioning flow, `ProvisioningBleService.h`, `JwtService.h`, `PreferencesManagerService.h` (NVS layout), and the wire models (`ProvisioningData.h`, `ProvisionRequest.h`, `GetLongLivedTokenResponse.h`) cross-checked against the live device-gateway contract verified in F3.2/F3.3 — all field names match exactly (`{macAddress,deviceType,version,capabilities}` in, `{deviceId,mqttToken,refreshToken,...}` out). **Found + fixed a real bug**: `PreferencesManagerService::LoadActionState()` returned `(char*)preferences.getString(...).c_str()` — a dangling pointer into a temporary `String`'s buffer, freed before the caller could read it (undefined behavior), _and_ skipped `preferences.end()` on the found-key path (NVS handle leak). This runs on **every boot for every command action** (`BaseCommandAction::loadState()`, called from `main.cpp` for all activated command capabilities) — restoring last-known state (e.g. outlet on/off) after a reboot. Fixed by returning `String` by value instead of a raw pointer into a dead temporary; call site (`BaseCommandAction.h:119`) was already assigning into a `String` so no caller changes needed. Rebuilt clean (`esp32s3_mini-test`, flash 96.4%/1851.5 KB). **Also noted, not fixed**: dead/unused efuse-derived `deviceID` buffer in `ProvisioningBleService::HandleProvisioning` (computed, never sent or used — `WiFi.macAddress()` is what's actually sent as `macAddress`); BLE chunk reassembly isn't implemented in `ProvisioningCallbacks::onWrite` (single-write assumption) — relies entirely on the mobile app negotiating a large-enough BLE MTU for the full JSON payload (JWT-sized `provisioningToken` included), unverified since the mobile client isn't in this repo. **Remaining for full acceptance**: real BLE pairing, NVS persistence, and WiFi provisioning behavior on actual hardware against staging (user's call — will test with real ESP32-S3 hardware once staging is up, not blocking further local work). |                                                                                                           |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| F3.8 | [~] | CI (`lattice-device-gateway`) + k8s base + staging deploy; ESP32 OTA CI alignment for v2.2 firmware (staging channel only). **device-gateway infra done** (`device-gateway-ci.yml`, `k8s/base/device-gateway`, staging overlay + ingress path-routing `/api/provisioning                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | device                                                                                                    | camera`→ device-gateway, kargo warehouse/stages). **Three OTA bugs (debugged 2026-06-16) — FIXED 2026-06-17:** (1) ✅`MqttActionsHandlerService.h`out-of-bounds: OTA topic now handled before any`parts[1/3/5/6]`access (+`parts.empty()`/`<7`guards). (2) ✅ Real root cause was the **gitops ota-manager init container** overriding the firmware image's`entrypoint.sh`with a dumb`cp`(no`<version>.bin`/`latest.json`), so the Kargo image path never announced — removed the `command:`override + set public`OTA_BASE_URL`per overlay; deleted the dead commented`/release`CI block. Firmware distribution is now fully **Kargo/image-driven** (firmware-staging → firmware-prod). (3) ✅ Deleted`esp32-ota-promote.yml`(prod promotion = Kargo`firmware-prod`). **Security hardening:** ota-manager is now **read-only** (no upload/`/release`endpoint — publishing is Kargo/image + init-container only), requires device`device_usage`JWT on`/download`+`/check`, and rate-limits all endpoints; firmware sends its JWT on OTA download (HTTPUpdate request callback). **Architecture: ota-manager no longer touches MQTT** — on startup it publishes each loaded `latest.json`to`q.ota.incoming`; digest validates/audits → `q.ota.dispatch`→`mqtt-service`(the sole MQTT owner) publishes the retained`ota/updates/<deviceType>`(this completes the previously-producerless OTA queue chain). ota-manager dropped its EMQX account + mqtt-certs; digest`SEMVER`now accepts the`v`-prefixed tags. **Also:** ota-manager moved into the npm workspace (`services/ota-manager`), now uses `@lattice/logger`+`@lattice/queue`, monorepo multi-arch Dockerfile, VSCode launch/debug (9234). Firmware builds green (`esp32s3_mini-test`, `esp32s3_cam-test`); both overlays render. **Still pending:** deploy + on-device OTA verification (sequencing: enable prod `/download` auth only after the fleet is on token-aware firmware). | real/sim device provisions against staging; OTA push reaches device and reboots to new version | F3.4, F2.9 |

| F3.9 | [ ] | OTA action migration: when `GET /config` detects `device.version !== requestedVersion` (TODO at `device-configuration.service.ts:35`), run a full migration instead of the in-place version update. Steps: (1) upsert `Device(type, newVersion)` row; (2) device must re-send capabilities after OTA reboot — preferred path: firmware re-calls the provisioning endpoint (already has a permanent JWT; provisioning upserts by MAC so it's idempotent), avoiding duplicate capability-declaration logic; (3) upsert `DeviceCapabilityBlueprint[]` for the new version; (4) re-point `UserDevice.device_type_id` → new Device row; (5) migrate `UserDeviceAction`: keep matching capabilities, add new ones (auto-activate defaults), remove orphaned ones. | provision a device, OTA it to a version with a new capability → new action appears in UI; removed capability disappears | F3.7, F3.8 |
| F3.10 | [ ] | PWM output action type: new `implementation_type = 'PwmOutputAction'` for LED dimming / fan-speed / servo control. Cross-cutting: (1) **Schema**: add `PwmOutputAction` to `DeviceAction.implementation_type` enum; command value = integer 0–100 (duty cycle %); (2) **ESP32 firmware**: `PwmOutputAction` class using LEDC peripheral — `setup()` configures channel + freq + resolution, `execute(value)` calls `ledcWrite()`; add to `DynamicDeviceActionsService` factory; (3) **device-gateway config**: no change — `implementation_type` returned as-is; (4) **UI**: action control renders as a slider (0–100) when `implementation_type = 'PwmOutputAction'` instead of a toggle. | provision a device with a PWM action; slide to 50% in UI → device dims LED to half | F3.7 |
| F3.11 | [ ] | Server-driven device reset on config change: when a user updates a device's action config (pin assignment, telemetry interval, etc.) via the api, the server — not the client — triggers the device to reload. Mechanic: after a config write, publish `q.action.dispatch` with a reserved `mqtt_action_name = 'system/reset'`; mqtt-service forwards it to the device's command topic; firmware handles it in `MqttActionsHandlerService` (`ESP.restart()` or re-runs `loadFromServer()` without full restart). Removes the client-side "reset device" button. | update a device's pin config via api → device reboots and re-fetches config with no client action | F3.3, F6.1 |
| F3.12 | [ ] | Device heartbeat: firmware publishes a periodic ping on `users/{userId}/devices/{deviceId}/heartbeat` (default interval: 30 s, configurable via NVS); mqtt-service routes the topic to a new `q.device.heartbeat` queue; digest-service consumes it, refreshes `device_online:{id}` cache TTL (90 s → 60 s, shorter than heartbeat interval × 2) and suppresses a socket `device_status_change` unless the device was previously offline (avoids noisy re-emits). Devices that miss two consecutive heartbeats (i.e. cache expires with no MQTT LWT received) are marked offline. This replaces the pure-LWT offline detection with a positive-presence model — LWT remains as a fast-path fallback on clean disconnect. Schema: no change (uses existing `UserDevice.online`). | provision a device → heartbeats arrive every 30 s; kill power (no LWT) → device shows offline within ~60 s; restore power → back online without page refresh | F3.4, F6.1 |
| F3.14 | [ ] | **Device version capability backfill job:** persistent migration-time job that walks every known firmware version in `device_models` and ensures its full capability set exists in `device_capability_blueprints`. Run as part of the `migration-job` init container after `migrate deploy` (before the catalog seed). Steps: (1) query all distinct `(device_type, version)` pairs from `device_models`; (2) for each version, check whether its capabilities are already present in `device_capability_blueprints`; (3) any missing version triggers a capability load — strategy: (a) if a firmware image for that version is available from the OTA store (`ota-manager` image-layer artifact), extract + run the manifest generator against it; (b) otherwise fall back to copying the closest known version's capabilities and flagging the row as `source = 'inferred'` so admins can correct it. This guarantees that after every DB migration (new column, schema change, etc.) the capability catalog is fully populated for the whole fleet history — not just the currently deployed firmware version. Schema: add nullable `source VARCHAR(32)` to `device_capability_blueprints` (`'firmware'` / `'inferred'` / `'seeded'`); new migration via `migrate diff`. **CI commitment:** the manifest generator output files (one JSON per `(device_type, version)` in `prisma/manifests/`) must be committed back to the repo by the firmware CI pipeline at build time — the pipeline runs the host-side manifest generator, then does a `git commit + push` of any new/changed manifest files before the Docker image is built. This means the migration-job always has a complete, version-stamped set of manifests baked into its image and never depends on runtime firmware extraction. | run the job against a DB that has two device versions with different capability sets → both have complete rows; a freshly introduced version with no manifests gets inferred rows flagged correctly; cutting a new firmware version in CI → manifest JSON appears as a committed file before the image is pushed | F3.8, F1.9 |
| F3.13 | [ ] | Firmware-authored command validation via `valid_parameters`: each ESP32 action class's `blueprint()` emits a `valid_parameters` descriptor (e.g., a regex string such as `^(on\|off)$` for a switch, `^([0-9]\|[1-9][0-9]\|100)$` for a 0–100 PWM, or a freeform JSON constraint object for complex types). Cross-cutting: (1) **Schema**: add `valid_parameters VARCHAR(512)` (nullable) to `device_capability_blueprints` — new migration via `migrate diff` (init frozen); (2) **ESP32 firmware**: add `validParameters()` (returns `const char*`) to each action class, included in the `CapabilityDescriptor` produced by `blueprint()`; (3) **Provisioning (device-gateway)**: store `valid_parameters` from the provisioning payload into `DeviceCapability` during upsert; (4) **`seed-catalog.ts`**: include `valid_parameters` in the seed manifests per capability; (5) **`GET /config` (device-gateway)**: include `valid_parameters` in the capability descriptor returned to the device (devices may self-validate before sending telemetry); (6) **api command dispatch** (`POST /actions/:id/execute` or equivalent): before publishing `q.action.dispatch`, validate `value` against the capability's `valid_parameters` — reject with 400 if invalid; (7) **google-home EXECUTE** (`services/google-home`): apply the same validation before dispatching — return `INVALID_VALUE` error code to Google if the value fails. `valid_parameters` is authored by the firmware and stored verbatim; the server treats it as a regex string (compiled at validation time) or falls back to no-op if null (backward-compatible). **Also fix `google_device_traits.valid_parameters`** (column exists in schema since `0_init`, was populated by the old `seed.ts` with values like `{"values":["on","off"]}`, now orphaned — `seed-catalog.ts` currently writes nothing to it): the manifest generator should emit accepted values per trait (e.g. `OnOff → ["on","off"]`), and `seed-catalog.ts` should write them to `google_device_traits.valid_parameters` via `upsertGoogleDeviceTrait`. The pipeline coordinator (`ml-router`) can then query these to enrich the LLM prompt with accepted command values instead of sending raw trait URIs. | provision a device → `valid_parameters` stored; send an out-of-range command via api → 400; send a valid command → dispatched; Google EXECUTE with bad value → INVALID_VALUE; re-seed → `valid_parameters` populated per capability; pipeline LLM prompt includes accepted values from traits | F3.2, F3.7, F2.6, F7.4 |

## F4 — E2E test harness (early — protects every later feature)

Existing detailed plan: `C:\Users\ishai\.claude\plans\ok-add-import-nifty-kazoo.md`. Snapshot has 23 provisioning + 15 VLM integration tests to salvage.

| #    | St  | Item                                                                                              | Test                                        | Needs                              |
| ---- | --- | ------------------------------------------------------------------------------------------------- | ------------------------------------------- | ---------------------------------- |
| F4.1 | [ ] | `compose.test.yaml` (full stack, +1 port offsets) + Jest `e2e/` workspace + global setup/teardown | `npm run test:e2e` boots & tears down stack | F2.9-ish (api+gateway Dockerfiles) |
| F4.2 | [ ] | Helpers: SimDevice (MQTT.js), TestSocketClient, DB factories, waitFor                             | helper self-tests                           | F4.1                               |
| F4.3 | [ ] | E2E: provisioning suite (re-land/adapt the 23 snapshot tests)                                     | suite green                                 | F4.2, F3.2                         |
| F4.4 | [ ] | E2E: telemetry round-trip (device → gateway → queue → ingest → socket)                            | suite green                                 | F4.2, F6.1                         |

## F5 — `backoffice` core UI

| #    | St  | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Test                                                                          | Needs      |
| ---- | --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ---------- |
| F5.1 | [x] | Theme foundation: ThemeService (light/dark, localStorage), CSS tokens, Material dual palette, navbar + mobile drawer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | visual check, both themes, mobile width                                       | —          |
| F5.2 | [x] | Shared domain models (`src/app/models/`) + auth service (Google popup → `POST /api/auth/google`, JWT decode) + login page                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | real login in browser                                                         | F2.2       |
| F5.3 | [x] | Socket service: `action_state_update` contract + status events                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | live state change visible in console/UI                                       | F2.7       |
| F5.4 | [x] | Dashboard + group tiles + group bottom-sheet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | browser walk-through with seeded data                                         | F5.2, F2.6 |
| F5.5 | [x] | Device mgmt pages (list w/ mobile cards, register, capability dialog)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | register a device end-to-end from UI                                          | F5.2, F3.2 |
| F5.6 | [x] | Routing + nav wiring for all landed pages                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | click-through                                                                 | F5.4       |
| F5.7 | [x] | Sort out backoffice CI: reconcile the "ci test" commits already on `master`, point workflow at `v2.2`, nginx Docker image, staging deploy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | UI served from staging URL, login works                                       | F5.2, F2.9 |
|      |     | **Narrowed 2026-07-03 audit**: `k8s/base/backoffice` + staging overlay ref + nginx image already exist and are wired. The only open gap was CI-trigger branch alignment. **Resolved 2026-07-07 — direction reversed (user call):** the `v2.2` working branch is abandoned (all rebuild work lands on `master` directly), so rather than add `v2.2` to `backoffice.yml`, the `'v2.2'` push trigger was **stripped from every workflow** that still carried it (`api-ci`, `checks`, `device-gateway-ci`, `digest-service-ci`, `mqtt-service-ci`, `socket-server-ci`) — all CI now triggers on `['master']` only. `backoffice.yml` was already master-only; gitops has no `v2.2` references. No `v2.2` trigger remains anywhere in either repo.                                                                                                                                                                                                                                                                                                                                     |                                                                               |            |
| F5.8 | [x] | Responsive UI / phone compatibility: full mobile-first pass across all landed pages (breakpoints, touch targets, drawer nav, mobile card layouts), verified at phone widths                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | walk every page at phone viewport in browser/devtools; usable on a real phone | F5.6       |
|      |     | **F5.1–F5.6/F5.8 audited 2026-07-03, not newly built this session**: this UI was already implemented earlier on `master` (predating this tracker) and never had its roadmap status updated. Verified by code audit: `ThemeService` (light/dark, localStorage, dual Material palette), `AuthService`/`LoginComponent` (Google popup → `POST /api/auth/google`, JWT decode), `DeviceSocketService` (`action_state_update$`, `deviceOnlineStatusChange$`, pending/failed observables), `UserDashboard`+group tiles+`GroupBottomSheetComponent`, `MgmtDeviceListComponent`/register/`DeviceConfigComponent`, `app.routes.ts` wired for all landed pages with auth/admin guards, responsive breakpoints (1024px/599px) + mobile bottom tab bar across dashboard/device-config/mgmt/automations. Also confirmed `environment.*` files point at the new v2.2 services (`api`, `device-gateway`, `socket-server`) with no leftover monolith-only routes. No `src/app/models/` directory specifically — domain interfaces are co-located in service files instead; treated as equivalent. |                                                                               |            |

> **F5.9/F5.10 moved to backlog (decided 2026-07-03)** — confirmed via code audit that neither exists yet (no admin users page, no user preferences page). Not required for F5 close-out; pick up whenever, same as F15.

| #     | St  | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Test                                                                                                                                            | Needs      |
| ----- | --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| F5.9  | [ ] | **Backlog.** Admin users page: list all users (name, email, role, type, last login), inline role/type edit, ban toggle, delete with confirm dialog. **Ban mechanic:** add `banned Boolean @default(false)` to `User` (new migration); `PATCH /api/users/:id` with `{banned:true}` writes DB + writes `banned:{userId}` key to Valkey (no TTL); `requireAppToken` middleware checks Valkey `banned:{userId}` **before** any DB call — O(1) check, instant effect without waiting for JWT expiry, no extra DB round-trip per request; unban = DB write + `DEL banned:{userId}`. Calls `GET/PATCH/DELETE /api/users` (F2.3 backend done; ban needs schema + middleware + Valkey additions). | CRUD + ban/unban in browser as admin; banned user gets 403 immediately on next request; unban restores access; non-admin cannot reach the route | F2.3, F5.6 |
| F5.10 | [ ] | **Backlog.** User preferences page: account settings (display name, password change), theme persistence (server-side, not just localStorage), timezone/locale. Leave a placeholder "Notifications" tab wired up at F15.5.                                                                                                                                                                                                                                                                                                                                                                                                                                                                | update display name → persists across sessions; theme preference survives logout                                                                | F2.3, F5.6 |

## F6 — Workers + RabbitMQ

| #    | St  | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Test                                                                                      | Needs      |
| ---- | --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ---------- |
| F6.1 | [~] | `services/digest-service`: authoritative DB writer + Valkey cache + socket emitter. `q.telemetry.arrived` resolves UserDeviceAction id **+ kind** (image vs scalar, via DeviceAction.implementation_type, cached in Valkey `action_resolve:{deviceId}:{action}` → `{id}:{kind}`). **Scalar** → current_state + sensor_history + `action_state:{id}` cache + socket + fan-out `q.rules.evaluate`. **Image** (TakePicture/LiveStream[Http]Action) → every frame to sensor_history (value column widened to TEXT) + `camera_frame:{deviceId}` cache (60s, for F8 pipeline pickup) + emitted to the UI as **`action_state_update` keyed by actionId** (the UI binds the camera display to an action and renders `action.state`; current_state is NOT written for images to avoid per-frame DB churn); pipeline trigger is a TODO for F8. `q.device.state.changed` → UserDevice.online + `device_online:{id}` cache + socket. `q.ota.incoming` → validate semver + audit + forward `q.ota.dispatch`. **`q.action.requested`** (F2.7; UI command intent by actionId) → resolve actionId → optimistic `current_state` + `action_state:{id}` cache + `action_state_update` echo, then publish `action.dispatch` (`{value,duration}` body) → mqtt-service → device (desired state; reconciled by telemetry). | F4.4 E2E / manual mosquitto_pub → row in DB + UI update                                   | F1.4, F3.4 |
| F6.2 | [x] | Automation worker: rules evaluation (conditions.ts) → action dispatch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | rule fires on threshold telemetry                                                         | F6.1       |
|      |     | **`services/automation-worker` (port 3008)** — consumes `q.rules.evaluate`, evaluates user rules (threshold / device_status / schedule conditions; vlm conditions stubbed pending F8), publishes `q.action.dispatch`. `node-cron` fires `evaluateScheduledRules` every minute. Dockerfile + `automation-worker-ci.yml` + `k8s/base/automation-worker` + staging overlay + kargo warehouse/stage. **Monolith teardown:** `rules.engine.service.ts` deleted; cron removed from `backend/index.ts`; `rulesEngineService.evaluateForUser()` + `skipRulesEval` removed from `action.hub.service.ts` (digest already triggers the worker via `q.rules.evaluate`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |                                                                                           |            |
| F6.5 | [ ] | Historic telemetry retention/cleanup: prune/aggregate `sensor_history` — scalar readings (downsample to averages) and **image/camera frames** (now stored in full as TEXT, grows fast). Define retention policy + scheduled cleanup job.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | old rows pruned/aggregated per policy; image history bounded                              | F6.1       |
| F6.6 | [ ] | Accept telemetry error readings (currently ignored): device-reported error/fault values in telemetry are dropped today — define an error-reading contract, persist + surface them (state/history + socket), don't silently discard.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | device emits an error reading → it's stored and visible in UI, not dropped                | F6.1       |
| F6.7 | [R] | Camera last-frame on load: image frames reach the UI live (`action_state_update` keyed by actionId) but `current_state` is intentionally NOT written for images (per-frame base64 = DB churn), so a camera card is blank on fresh load until the next live frame (noticeable for on-demand TakePicture). Serve the latest frame on load from the `camera_frame:{deviceId}` Valkey cache (or newest `sensor_history` row) via an endpoint — **not** by reintroducing per-frame `current_state` writes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | open dashboard with an idle camera → last frame shows immediately, no per-frame DB writes | F6.1       |
| F6.3 | [x] | Rules CRUD in api + rules UI page + rule editor dialog                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | create rule in UI → it fires                                                              | F6.2, F5.6 |
|      |     | **Audited 2026-07-03 (pre-existing, undocumented)**: `services/api/src/routes/rules.routes.ts` + `rules.service.ts` full CRUD (list/create/update/toggle/delete + `GET /events?emergency=true`) against `UserRule` (name, is_emergency, condition_operator, cooldown_seconds, conditions incl. threshold/device_status/schedule, actions). Backoffice `rule-editor-dialog` component wired to the api with condition/action summaries. **VLM condition type has no UI yet** (stubbed pending F8 LLM/VLM pipeline integration) — everything else works.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |                                                                                           |            |
| F6.4 | [x] | CI (`lattice-workers`) + k8s base + staging deploy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | rule round-trip on staging                                                                | F6.2       |
|      |     | **Audited 2026-07-03**: this infra already shipped as part of F6.2 (`automation-worker-ci.yml`, `k8s/base/automation-worker`, staging overlay ref, `automation-worker-staging` Kargo stage with `autoPromotionEnabled: true`) — F6.4 was just never checked off separately.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |                                                                                           |            |

## F7 — `google-home` service (port 3010)

**Service extracted from backend monolith (2026-06-17).** SYNC/QUERY/EXECUTE webhook and HomeGraph reporting are now in `services/google-home`. OAuth account-linking stays in `backend` for now (moves to `api` at F2). Port bumped to 3010.

| #    | St  | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Test                                                                                                                                | Needs      |
| ---- | --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| F7.1 | [x] | OAuth account-linking routes (`/auth`, `/token`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Google account-linking flow in test project                                                                                         | F2.2       |
|      |     | **Audited 2026-07-03 (pre-existing, undocumented)**: `services/google-home/src/routes/google.auth.routes.ts` implements the full flow — `GET /auth` (validates client_id/redirect_uri/response_type, renders login), `POST /auth/login` (username/password or Google code, issues a short-lived oauth_code in Valkey, 600s TTL), `POST /token` (exchanges code for access/refresh via constant-time secret check, or refreshes). `auth.service.ts` handles Google code exchange + user upsert. End-to-end complete.                                                                                                                                                                                       |                                                                                                                                     |            |
|      |     | **Login moved to the backoffice 2026-07-21**: the service no longer renders its own login page or verifies credentials — deleted `views/auth.view.ts`, `services/auth.service.ts` and `POST /auth/login` (bcryptjs + google-auth-library dropped from the service). `GET /auth` now parks the OAuth params in Valkey under an opaque id (`oauth_req:<id>`, 600s) and 302s to `${BACKOFFICE_URL}/#/login?google_link=<id>`; the themed Angular login page finishes via `POST /auth/authorize` (app_usage JWT + requestId → `{ redirectUrl }`). One login UI, one password path, credentials never reach this service. Empty `BACKOFFICE_URL` = same origin (the deployed ingress serves both on one host). | full chain drove green locally: `/auth` 302 → authorize → `/token` exchange; UI verified light + dark, signed-out and "continue as" |            |
| F7.2 | [x] | SYNC intent — `google.sync.device.service.ts` ported; `willReportState: true` now that HomeGraph consumer exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Google Home test suite / simulator                                                                                                  | F7.1       |
| F7.3 | [x] | QUERY intent — `google.state.service.ts` ported; returns current DB state for all user actions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | simulator                                                                                                                           | F7.2, F6.1 |
| F7.4 | [x] | EXECUTE — `google.execute.device.ts` ported; dispatches `ACTION_DISPATCH` directly to RabbitMQ via `@lattice/queue` (no `actionHubService` dependency)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | voice/simulator command moves a real device                                                                                         | F7.3       |
| F7.5 | [x] | HomeGraph reportState — `q.action.result` consumer calls `homegraph.devices.reportStateAndNotification` after each device ack; **enabled** (was disabled/stubbed in backend)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | state change reflected in Google Home app                                                                                           | F7.4       |
| F7.6 | [x] | Infra — Dockerfile, `k8s/base/google-home`, added to staging+prod overlays, Kargo warehouse+stages (auto-promote staging, manual prod), `compose.yaml`, VSCode launch+tasks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `docker compose build google-home` green; `GET /health` 200                                                                         | —          |
| F7.8 | [x] | **Validate + cleanup** — `docker compose build google-home` green; `GET /health` 200; backend build clean without smarthome route; `actions-on-google`+`googleapis` already absent from `backend/package.json`; Traefik staging ingress rule `/api/google/smarthome` → google-home added                                                                                                                                                                                                                                                                                                                                                                                                                  | docker build + health check; backend build clean; kustomize renders                                                                 | F7.6       |
| F7.7 | [ ] | Separate Google client between test and prod: distinct OAuth client IDs/secrets + account-linking/redirect URIs per environment (env-config + sealed secrets), so staging never uses prod's Google credentials                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | staging uses test client, prod uses prod client; both link/sign-in independently                                                    | F7.1, F2.2 |

## F8 — Pipelines + `ml-router` (queue-based design)

**Do NOT re-land the snapshot's HTTP version.** Build per the approved async design (→ Appendix A): per-stage queues, per-model queues, SensorBlueprint.

| #    | St  | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Test                                            | Needs      |
| ---- | --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | ---------- |
| F8.1 | [x] | Pipeline + stage CRUD in api (+ SensorBlueprint validation) + pipelines UI page                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | create pipeline in UI                           | F5.6       |
|      |     | **Audited 2026-07-03 (pre-existing, undocumented)**: `services/api/src/routes/pipelines.routes.ts` + `pipelines.service.ts`/`pipelines-runs.service.ts` full CRUD (list/get/create/update/toggle/delete + run history) for Pipeline/PipelineStage/PipelineSensor/PipelineTrigger. **Validation approach differs from the F1.7/Appendix A plan**: no separate `SensorBlueprint` table — `pipelines.validation.ts` validates directly off `DeviceCapability.implementation_type` (image-capable vs sensor-only capabilities, ≤1 image input per pipeline, stage ordering — llm infer requires a preceding enrich stage). Achieves the same guarantee the blueprint was meant to provide; **F10's blueprint tier still stands** as a separate abstraction (templates/derive), not superseded. Backoffice `pipelines` list + `pipeline-editor-dialog` (stage/sensor/trigger forms) + `pipeline-run-history` + `pipeline-simulate-dialog` all wired to the api.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |                                                 |            |
| F8.2 | [x] | Pipeline worker: 4 consumers (trigger, sensor_digest, command_exec, finalization) + advancePipeline                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | sensors-only pipeline runs E2E                  | F6.1, F8.1 |
|      |     | **Corrected 2026-07-03** (an earlier pass on this same date wrongly marked this `[~]`/missing — verified directly against the code after user pushback, and the earlier check was wrong: it grepped for literal consumer names and missed the actual files). `services/ml-router/src/pipeline/coordinator.ts` is a single stateful coordinator, not 4 separate queue consumers — a deliberate, valid shape: `onTrigger`/`onCancel`/`onStageDone` consume `PIPELINE_TRIGGER`/`PIPELINE_CANCEL`/`PIPELINE_STAGE_DONE`; `advance()` runs the **enrich** stage (`pipeline/stages/enrich.ts` — the sensor_digest equivalent: builds `current_state`, sensor-history digest with compression (average/last_n/min_max/min_max_avg), `available_actions` enriched with trait `valid_parameters`, and live camera capture via `requestPicture` with a `sensor_history` fallback) and **command_exec** stage (`pipeline/stages/command-exec.ts` — validates action ownership against `run.userId` before dispatching, then publishes `ACTION_REQUESTED` per action) synchronously in-process; only the actual ML **infer** stage goes out to a per-model queue (`mlStageRK`) since that's the only stage needing independent backpressure/DLQ/scaling per the original Appendix A rationale. **finalization** is `finish()` — writes `PipelineRun.status` + publishes `PIPELINE_RESULT` + emits the socket run-update. Full dry-run/simulate support (`isDryRun`/`sensorOverrides`) threaded through enrich + command_exec, backing the `pipeline-simulate-dialog` UI. User confirmed this works by testing it directly. |                                                 |            |
| F8.3 | [x] | ml-router service: `setupModelQueues()`, model registry, ONNX runtime                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | model queue asserted; health check lists models | F1.4       |
| F8.4 | [x] | VLM handler (YOLO ONNX, classes: immature/mature/damaged) + LLM handler (ollama qwen2.5vl:7b); both wired to generic `infer:jobs`/`infer:response` Redis entrypoint and RMQ model queues                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | image pipeline E2E with camera frame            | F8.3, F3.6 |
|      |     | **VLM parked 2026-07-28**: with LLM inference moved external (multimodal-capable, e.g. `gemini-2.5-flash`), the local YOLO/VLM stage is no longer worth its training/maintenance cost for generic pipelines. A camera item's frame now flows from enrich straight to the multimodal LLM (`ml-executor` attaches `context.image`; `prompt.ts` adds a vision instruction only when a frame is present). The `vlm` model kind, `OnnxVlmProvider`, and the coordinator's VLM-stage branch are **kept intact** — only the `yolo-lettuce` row was removed from `models.json` (also resolving the F8.5 generic-naming violation) and the editor's Vision-model control hidden. Re-enable: add a `vlm` entry back to `models.json` + un-hide the control (+ restore the stage push/validation in `pipeline-editor-dialog`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |                                                 |            |
| F8.5 | [~] | CI + k8s (ml-router PVC for models) + staging deploy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | pipeline round-trip on staging                  | F8.2, F8.4 |
|      |     | **Audited 2026-07-03**: CI (`ml-router-ci.yml`, `ml-executor-ci.yml`), `k8s/base/ml-router` + `k8s/base/ml-executor`, both staging **and** prod overlays, and Kargo warehouses+stages (`ml-router-staging`/`ml-executor-staging`, `autoPromotionEnabled: true` in `kargo/project.yaml`) all already exist. **No PVC** — the roadmap called for one, but the actual approach is simpler: the YOLO ONNX model is baked directly into the `ml-executor` image at build time (`Dockerfile:50 COPY .../models ./models`), so no volume is needed unless models start changing independently of a code deploy. Staying `[~]` because the one thing not yet verified is the actual test criterion — **a pipeline round-trip has not been run against the staging cluster** (only local compose). Separately noticed, not blocking: the model path is `services/ml-executor/models/yolo-lettuce/v1/yolo.onnx` — `yolo-lettuce` violates the generic-naming rule (no farm/crop terms in platform code); worth a rename pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |                                                 |            |

## F9 — Emergency alerts

| #    | St  | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Test                           | Needs      |
| ---- | --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ | ---------- |
| F9.1 | [~] | Emergency rules CRUD (api) + gateway fast-path evaluation (cache)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | threshold breach → alert event | F3.4       |
|      |     | **Audited 2026-07-03**: the CRUD half is already done — emergency was merged into rules at F1.7/F6.3 (`UserRule.is_emergency` + `GET /api/rules/events?emergency=true`), so no separate emergency CRUD is needed. **The fast-path/cache half does not exist**: no low-latency emergency evaluation in `device-gateway` or `mqtt-service` — emergency rules currently flow through the normal `q.rules.evaluate` → automation-worker path (same as any other rule), with no bypass for latency. Remaining scope for this item narrows to: build the fast-path evaluation only. |                                |            |
| F9.2 | [ ] | Emergency UI page + `emergency_alert` socket event                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | alert appears in UI            | F9.1, F5.6 |

## F10 — Blueprints

> **Redesigned 2026-07-20 → [docs/plans/blueprints-redesign.md](docs/plans/blueprints-redesign.md).**
> The 2026-07-10 design was built, disliked, and shelved (`wip/blueprints-and-ai-rulegen-shelved-2026-07-13`;
> post-mortem [docs/plans/blueprints.md](docs/plans/blueprints.md)). It is **not** being revived —
> the redesign supersedes it. Key differences resolved with the user:
>
> - **Slots bind through sealed templates**, not `capability_key`/device-pin dual mode. A slot
>   points at a released `SealedTemplate`; derive reuses its version-range match + `materializeForUserDevice`.
>   No `DEVICE_ROLE_STR`, no `devices.role_tag`, no firmware change. Non-sealed slots are out of scope.
> - **Reconcile, not snapshot re-derive.** A blueprint is desired state; releasing a new version
>   flows structural changes into live instances (create/update/disable, never delete), skipping any
>   user-edited entity (`user_modified`) and never touching user overrides.
> - **Parameterize, don't mutate.** Derived automations store `@param.x`/`@phase.x` references
>   resolved at evaluation time (precedence: user override → current phase → blueprint default), via
>   a shared `packages/params` used by automation-worker, api validation, and ml-router (pipeline
>   prompts + sensor ranges). Phase advance = one column write; reconcile/phase/user-edit write
>   disjoint places and cannot clobber each other.
> - **Areas are a standalone primitive.** "These devices belong together" is a user-createable
>   `Area`, not welded to derivation. Grouped notifications + dashboard sectioning + AI scope key
>   off it. A derive creates one. Ships first, independently.
> - **Phases auto-advance** on elapsed duration (rides automation-worker's 10s cron).
>
> Generic naming holds: domain-specific blueprints are runtime admin content only — never repo
> seeds/code/tests. Build order below matches the redesign doc §13; the builder UI is deliberately last.

| #      | St  | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Test                                                                                                                                      | Needs          |
| ------ | --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| F10.0  | [x] | **Areas** — standalone `Area` primitive (`area_id?` on `UserDevice`/`Scene`/`UserRule`/`Pipeline`), CRUD service, dashboard sectioning, device assignment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | two areas created, sim devices assigned + drag-moved, sections ordered by `sort_order` ✓ (2026-07-21)                                     | F5.6           |
| F10.1  | [x] | `packages/params` resolver (`@param.`/`@phase.`) — library + 34 unit tests, shared `buildParamContext`, wired into `rules.engine` **and** ml-router (`enrich` sensor bounds + `buildPrompt`), all fail-closed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | derived rule fired with refs resolved; pipeline prompt + expected-ranges retuned on phase change with zero pipeline writes ✓ (2026-07-22) | F10.0          |
| F10.2  | [x] | Blueprint definition schema (slots→sealed templates, params, phases+targets, scene/rule/pipeline templates) + one vertical seeded as JSON; publish validation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 17 tables + ERD verified vs live DB; import → publish 200; bad refs/slots → 400 naming all 7 ✓ (2026-07-21)                               | F10.1          |
| F10.3  | [x] | Derive service — auto-bind via sealed template, create Area + bindings, materialize templates (refs verbatim). Reads the config sealed materialization already wrote; does not re-materialize                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | derive via curl → 2 slots auto-bound, area + 1 scene + 2 rules + 1 pipeline; derived rule fired ✓ (2026-07-21)                            | F10.2          |
| F10.4  | [x] | Phases — targets, resolution precedence, manual `PUT /instances/:id/phase`, auto-advance cron (every minute), overrides API + instance read endpoint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | e2e: phase → default → override precedence with the rule row byte-identical across all three ✓ (2026-07-21)                               | F10.3          |
| F10.5  | [x] | Standalone Scenes: `Scene`/`SceneMember` model + `POST /api/scenes/:id/execute` fan-out via existing `ACTION_REQUESTED` + dashboard scene tiles (prerequisite for derive)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | scene over 2 sim devices → both ack ✓ (2026-07-20)                                                                                        | F2.6           |
| F10.6  | [x] | Overrides + drift + reconcile — publish flows structure into live instances, skips `user_modified`, disables removed entities, surfaces drift with per-entity reset                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | e2e + live: v2 kept the user's edited rule (name + cd=900), updated the untouched one, added a new one, reset restored ✓ (2026-07-22)     | F10.4          |
| F10.7  | [x] | Grouped notifications — optional `context: { area_id, area_name }` on `NOTIFICATION_SEND` (`@lattice/queue` types + zod) + one title prefix in `templates.render`; producers: rule fired, phase advanced, reconcile                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | area-tagged events render as "Area · Title" ✓ (2026-07-22)                                                                                | F10.0, F15.1   |
| F10.8  | [x] | Blueprint gallery + derive wizard + instance page (bindings, phase timeline, params, drift)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | driven headlessly: gallery readiness, derive dialog, phase click 40→20, override 33, restore → 20 ✓ (2026-07-22)                          | F10.3, F5.6    |
| F10.9  | [x] | Admin blueprint **form** builder — repeatable sections for slots/params/phases/rules/scenes/pipelines, slot+action dropdowns fed from the chosen sealed template, `{}` parameter inserters on every reference-bearing value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | built a blueprint from empty in the form; round-trips a saved one losslessly (Valid straight after save) ✓ (2026-07-22)                   | F10.8          |
| F10.10 | [ ] | **Sealed-template change propagation** — a blueprint addresses actions as `(slot_key, mqtt_action_name)` resolved at derive/reconcile time. A new template _version_ (same entries) is safe, but **renaming an entry's `mqtt_action_name`, or retiring an entry**, silently orphans every published blueprint that references it: publish validation only guards the _builder_, and there is no reverse lookup from a `SealedTemplate` to its dependent blueprints. A published blueprint keeps the stale string and degrades quietly at the next derive/reconcile (that entity resolves to nothing → skipped). Sealed templates deprecate-not-delete for _device bindings_, but that doesn't cover a rename. **Build:** reverse dependency check — on a sealed-template entry rename/removal, warn or block when a **published** blueprint references it, naming the affected blueprints; optionally re-point `(slot_key, action_name)` on reconcile. Interim safe rule: deprecate-and-add, never rename. | rename a referenced entry → warned, naming the blueprint(s); retire one → blocked or flagged; an unrelated template edit passes untouched | F10.9, catalog |

| F10.11 | [x] | **Per-phase user overrides** — `phase_key` on `blueprint_param_overrides` (`''` = all phases), precedence `phase override → all-phases override → phase target → default` written once as data in `packages/params` and reported back via `resolveParamWithSource`, instance view resolves **every** phase, settings rows expand to a per-phase breakdown | 5 new resolver cases (phase-scoped applies only in its phase, specific beats global, `@param.` unaffected, inert when not in that phase, source labels); live: tuned a phase the instance was not in ✓ (2026-07-31) | F10.4 |
| F10.12 | [ ] | **User global params page** — a user-level settings surface for config that is genuinely global to the _user_, not to one setup (units/locale, quiet hours, default notification channel, shared thresholds a user wants applied across every instance). Today the only user-writable config layer is `blueprint_param_overrides`, which is per instance by design (F10.11) — so anything global has nowhere to live and gets retyped per setup. **Build:** a `user_params` (or settings) table keyed by user, a page under the account/settings area, and a decision on whether blueprint params may fall back to it — i.e. whether the resolution chain gains a **user default** layer _beneath_ the blueprint default, which would let "I always want this" hold without pinning every instance. Per-instance overrides must keep winning over it | set a global value, derive two setups, both pick it up; override one instance and the other is unaffected | F10.11 |

## F11 — AI rule generator

> **Design plan sketched 2026-07-10** (the standalone `ai-rule-generator.md` was never committed;
> this blockquote is the current spec — the F10 blueprint context provider feeds it via F11.5).
> Chat-first: a `rule-builder` chat mode over the existing chat stack (socket-server → ml-router →
> ml-executor) with a real context-provider enricher replacing the Noop stubs; the LLM emits
> `CreateRuleDto`/`CreatePipelineDto` JSON, extracted by the (currently stubbed)
> `audit/response.processor.ts` into a new `AutomationSuggestion` table (suggestions never
> evaluated); approval materializes via the existing validated create services. **Builder model is
> user-selectable** (allowlist-validated `ChatIntentPayload.model` override + `GET /api/ml-models`
> picker). Blueprint context plugs in post-F10 as one more provider (loose coupling — neither
> feature blocks the other). Supersedes the earlier `POST /api/ai/rules/generate` one-shot sketch.

| #     | St  | Item                                                                                                                                                                              | Test                                                     | Needs             |
| ----- | --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ----------------- |
| F11.1 | [ ] | `AutomationSuggestion` schema + builder-model plumbing (`ChatIntentPayload.model`, plan `allowModelOverride`, registry validation, `GET /api/ml-models`)                          | chat with override → chosen provider runs the inference  | F8.3              |
| F11.2 | [ ] | `rule-builder` chat mode: context-provider enricher (devices+actions w/ `valid_parameters`, sensor summary, existing automations) + system prompt + response-processor extraction | scripted chat → persisted, zod-valid suggestions         | F11.1             |
| F11.3 | [ ] | Suggestions API: inbox, approve (materialize via existing rules/pipelines create services), reject-with-feedback, revision supersede chain                                        | curl approve → live rule fires; reject → nothing created | F11.2, F6.3, F8.1 |
| F11.4 | [ ] | UI: Automate-tab entry + model picker + inline suggestion cards + review panel + pipeline dry-run ("test before approve") + "Improve with AI" on existing entities                | full flow in backoffice against dev stack + device-sim   | F11.3, F5.6       |
| F11.5 | [ ] | Blueprint context provider (consumes `buildInstanceContext`, post-F10 derive)                                                                                                     | instance phases/notes appear in enrich messages          | F11.2, F10.2      |

## F12 — Import/Export + reference seeds

| #     | St  | Item                                                                                               | Test                               | Needs |
| ----- | --- | -------------------------------------------------------------------------------------------------- | ---------------------------------- | ----- |
| F12.1 | [ ] | 5 import endpoints (catalog, blueprints, rules, emergency, pipelines) — thin routes, service logic | import seed JSONs via curl         | F10.1 |
| F12.2 | [ ] | Export/Import buttons on the 5 pages + `seeds/` reference files                                    | export → wipe → import → identical | F12.1 |

## F13 — Observability

| #     | St  | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Test                                          | Needs |
| ----- | --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- | ----- |
| F13.1 | [x] | Wire `packages/otel` + structured logs through all services                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | traces visible locally (collector in compose) | F1.3  |
|       |     | **Completed 2026-07-07.** Instrumentation hooks (`initOTel(name)` + `pinoMixin` log correlation) were already wired in the 9 TS services and structured logs (`@lattice/logger`) everywhere — but the wiring was **inert**: `initOTel` registered no instrumentations (→ zero spans, empty traces, `pinoMixin` never populated) and there was no collector in compose. Closed the gap: (1) `@lattice/otel` now registers `@opentelemetry/auto-instrumentations-node@0.55` (fs instrumentation disabled — too noisy), so http/express/amqplib/pg/redis spans flow automatically; (2) added **Jaeger all-in-one** to `compose.yaml` (UI `:16686`, OTLP `:4318/:4317`); (3) set `OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318` on all 10 app services (8 in `compose.dev.yaml`, google-home + ota-manager in `compose.yaml`); (4) wired the 10th service **ota-manager** (JS) — `initOTel('ota-manager')` hoisted above `express` so auto-instrumentation patches it, plus a `/metrics` route. Verified: otel builds, `initOTel` starts + emits a span with an endpoint set, merged compose config valid with jaeger + endpoint on 10 services. Traces export only when the endpoint is set (no-op otherwise, per F1.3), so prod/staging behaviour is unchanged. |                                               |       |
| F13.2 | [x] | k8s observability stack (OTel Collector → Tempo/Loki/Prometheus → Grafana) on staging                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Grafana shows a request trace                 | F13.1 |

## F15 — `notification-service` (multi-channel user notifications)

Centralized delivery of user-facing notifications over multiple channels (email, SMS,
in-app, push). Queue-driven: any service publishes `q.notification.send`; this service owns
channel fan-out, user preferences, templating, dedupe/rate-limit, and history. Lands before
F14 but isn't on the cutover critical path.

> **Status: IN PROGRESS (picked up 2026-07-08).** Full-group build (F15.1–15.8), landing in 3
> chunks (see [F15-notification-service-plan.md](F15-notification-service-plan.md)).
> **Decisions locked 2026-07-08:** v1 channels = **in-app + email + push** (SMS reserved as a
> no-op stub); email = **SMTP via nodemailer**; push = **web-push / VAPID** (only client is the
> Angular backoffice — revisit FCM when a native app exists); dedupe window = **5 min** default;
> default prefs (no row) = in-app on for all, email on for `emergency`+`email_verification`,
> push off until subscribed. **Chunk 1 landed (uncommitted):** queue contract
> (`RK.NOTIFICATION_SEND`/`QUEUES.NOTIFICATION_SEND` + binding via new `assertNotificationQueues()`
>
> - `notificationSendSchema` + `NotificationSendPayload`), `SOCKET_EVENTS.NOTIFICATION` in
>   `@lattice/ioredis`, schema (`NotificationPreference`, `NotificationHistory`,
>   `User.email_verified`+`email_verification_token`) with migration `20260708150000_notification_service`
> - `SCHEMA.md` ERD/examples, and the `services/notification-service` skeleton (port 3011 — 3009 is ml-router: express,
>   env, `/health` reporting adapter status, `Channel` interface + 4 adapters, log-only consumers
>   for `notification.send`/`notification.publish` pending F15.3/F15.4). Green: typecheck, lint,
>   193 unit+sanity tests. **BACKLOG (deferred 2026-06-28).** **Plan decisions (locked when picked up):** (1) new
>   `services/notification-service` (port 3009) = stateless consumer + channel adapters;
>   **prefs/history CRUD lives in `api`** (F2 owns user DB writes), both share
>   `@lattice/prisma-client`. (2) In-app channel **reuses the socket path** —
>   `@socket.io/redis-emitter` → `user_{userId}` rooms, new event `notification` (no new
>   infra). (3) Adapters behind a common `Channel` interface; each loads its provider only
>   if creds present, else **no-op/log adapter** (runs without Twilio/FCM/SMTP). (4)
>   **Dedupe + rate-limit in Valkey** keyed by `(userId, eventType)`. (5) **Payload-driven
>   templating** — producer sends `eventType` + `data`; service renders per-channel.
>   **Pre-work (F15.0):** `RK.NOTIFICATION_PUBLISH`/`QUEUES.NOTIFICATION_PUBLISH` already added
>   to `packages/queue`; `digest-service` publishes `notification.publish` on OTA events
>   (F6.1 done) — F15 must declare + bind `q.notification.publish` on startup and fan-out
>   to per-user `q.notification.send` messages after resolving device owners + prefs.
>   Also add `RK.NOTIFICATION_SEND`/`QUEUES.NOTIFICATION_SEND` + binding +
>   `NotificationSendPayload {userId,eventType,data,dedupeKey?,channels?}` to
>   `packages/queue`; add `NotificationPreference` (`@@unique[user_id,channel,event_type]`)
>
> * `NotificationHistory` (`@@index[user_id,created_at]`) — **a NEW migration via
>   `migrate diff`** (init is frozen). Dockerfile must copy the service's nested
>   `node_modules` (nodemailer/twilio won't hoist — [[reference-service-dockerfile-nested-deps]]).
>   **Open questions to resolve at pickup:** v1 channels (recommend in-app + email first);
>   default prefs for users with no row; concrete providers (SMTP host / Twilio / web-push
>   vs FCM); dedupe window default.

| #     | St  | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Test                                                                                                                                                                                            | Needs              |
| ----- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| F15.1 | [R] | Service skeleton: express app, `env.config.ts`, exception middleware, `/health`; channel adapter abstraction (email via SMTP/provider, SMS via provider e.g. Twilio, in-app via socket emit, push via FCM/web-push) — providers behind a common interface, no-op/log adapter when creds absent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `curl /health` → 200; each adapter loads with and without creds                                                                                                                                 | F1.1               |
| F15.2 | [R] | Schema + api: user notification **preferences** (per-channel opt-in, per-event-type) + **notification history** table; CRUD in api + read for in-app inbox                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | set prefs via curl; rows persist; history queryable                                                                                                                                             | F2.3, F1.8         |
| F15.3 | [R] | Queue consumer `q.notification.send` → resolve user prefs → fan out to enabled channels; payload-driven templating, dedupe + rate-limit per (user, event-type)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | publish `notification.send` → enabled channels fire, disabled skipped, dupes suppressed                                                                                                         | F1.4, F15.1, F15.2 |
| F15.4 | [~] | Producers wired: emergency alerts (F9), rule fires (F6), device offline (digest `q.device.state.changed`), OTA outcome — publish `q.notification.send` instead of (or alongside) direct socket                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | trigger each source → notification delivered per prefs                                                                                                                                          | F15.3, F9.1, F6.2  |
|       |     | **Chunk 2 landed 2026-07-10 (uncommitted, awaiting review).** **F15.2:** api `notifications.service.ts` + `notifications.routes.ts` (`/api/notifications` — prefs matrix GET/PUT with defaults+locks+transactional exclusion, inbox list w/ cursor, unread-count, mark `:id`/all read). **F15.3:** notification-service `delivery/` — `preferences.ts` (row-overlay-on-default + transactional bypass), `templates.ts`, `dedupe.ts` (Valkey SET NX, 5-min TTL), `recipient.ts`, `dispatcher.ts` (dedupe→resolve→render→fan-out→history-when-inapp); consumer runs the real path. **F15.4:** OTA `notification.publish`→owner fan-out (`fanout.ts`), automation-worker publishes `rule_fired`/`emergency` on fire (rule-scoped dedupe), digest publishes `device_offline` (device-scoped dedupe). Shared catalog `@lattice/queue/notifications.ts` (channels, event types, default matrix, transactional set). Local wiring: `compose.dev.yaml` service (**port 3011** — 3009 is ml-router), Dockerfile, VSCode launch+task+workspace folder. Tests: `platform.notifications.test.ts` + TEST-PLAN entry. **Verified:** build:libs, typecheck, lint, 194 unit tests green; migration applies clean on a fresh DB (both tables, no drift). **Still open:** emergency (F9) has no dedicated producer (only rule-engine `is_emergency`); no live end-to-end run (stack runs old images); push-subscription storage + real VAPID = chunk 3. |                                                                                                                                                                                                 |                    |
| F15.5 | [R] | In-app inbox UI (notification center + unread badge, mark-read) + preferences page                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | notifications appear in UI; toggle a channel off → it stops                                                                                                                                     | F15.2, F5.6        |
|       |     | **UI landed 2026-07-10 (uncommitted, awaiting review).** `backoffice` `NotificationsService` (root: `/api/notifications` HTTP + live `notification` socket event + `unreadCount`/`items` signals; **lazily** wires the socket via `Injector` only once authenticated, so it never constructs `DeviceSocketService` with a null token on the login page). `NotificationsComponent` at `/notifications` — mat-tabs **Inbox** (list, unread dot, mark one/all read, empty state) + **Preferences** (event×channel slide-toggle matrix; locked cells disabled; optimistic save via PUT). Shell wiring: sidebar link with unread badge (visible when collapsed), profile-menu entry (mobile reach) with badge, `connectLive()`+`refreshUnread()` primed on load when authenticated. `DeviceSocketService` gained a `notification$` observable. **Verified:** `ng build` (dev) + `ng lint` clean. **Not done:** no live render against the stack (running api image lacks `/api/notifications`); verify-email/reset-password pages (F15.8/15.9 UI).                                                                                                                                                                                                                                                                                                                                                                                         |                                                                                                                                                                                                 |                    |
| F15.6 | [~] | CI (`lattice-notification-service`) + k8s base + staging deploy (provider creds as sealed secrets)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | end-to-end notification on staging                                                                                                                                                              | F15.3              |
| F15.7 | [R] | Schema: add `email_verified Boolean @default(false)` + `email_verification_token String? @unique` to `User`; **new migration via `migrate diff`** (init is frozen). Google-registered users get `email_verified: true` on creation (Google already verified).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `prisma migrate deploy` applies cleanly; `email_verified` readable on the user row                                                                                                              | F1.8               |
| F15.8 | [R] | Registration email verification flow: (1) **Register** no longer issues a JWT — instead saves a `crypto.randomUUID()` token, publishes `q.notification.send` (`eventType: email_verification`, `data: { username, verifyUrl }`) → notification-service renders the email; returns `{ pendingVerification: true }` (HTTP 202). (2) **`GET /api/auth/verify-email?token=`** clears the token + sets `email_verified: true` + returns a full `app_usage` JWT + user object (so the UI lands the user straight into the app). (3) **`POST /api/auth/resend-verification`** (body: `{ email }`, auth rate-limiter) — regenerates token, re-publishes `q.notification.send`; 404 if no pending user, 409 if already verified. (4) **Login gate**: `loginWithCredentials` checks `email_verified`; if false throws `403` with `{ error: 'email_not_verified', email }` so the UI can offer the resend flow. Google login auto-sets `email_verified: true` on `createGoogleUser` (no gate needed). (5) **UI (backoffice)**: post-register "check your inbox" page (shows email, resend button) + `/verify-email?token=` landing page that calls the verify endpoint and redirects to dashboard on success.                                                                                                                                                                                                                                    | register → inbox page shown (no JWT); click link → lands on app logged in; credential login with unverified account → 403 + resend offered; resend → new email arrives; Google login unaffected | F15.3, F15.7, F2.2 |

| F15.9 | [R] | **Password-reset flow (added 2026-07-10).** `POST /api/auth/forgot-password` `{ email }` → always 202 (never leaks account existence); credential accounts get a `crypto.randomUUID()` token + 1h expiry and a `q.notification.send` (`eventType: password_reset`, `data: { username, resetUrl }`). `POST /api/auth/reset-password` `{ token, password }` → validates token+expiry, sets the new hash, clears token; returns 204 (no auto-JWT — a leaked link can't also grant a session). Schema adds `password_reset_token`/`password_reset_expires` (folded into the notification migration). UI: "Forgot password?" link → `/forgot-password` request page + `/reset-password?token=` page. | forgot → email; reset link sets new password; login works with it; unknown email still 202 | F15.3, F2.2 |

> **Chunk 3 landed 2026-07-10 (uncommitted, awaiting review).** **F15.6:** CI
> `notification-service-ci.yml` (mirrors socket-server, multi-arch by-digest + version-tag bump);
> gitops `k8s/base/notification-service` (Deployment+Service, port 3011, DB/RabbitMQ/Valkey secrets,
> SMTP/VAPID from an optional `notification-secret` so the pod starts creds-less), added to the
> staging overlay (base + image pin) + Kargo warehouse/stage (`notification-service-staging`,
> `autoPromotionEnabled`). Prod overlay untouched (deferred). **Sealed secrets not created** (need
> the cluster key). **F15.7/F15.8/F15.9:** api register→verify rework, login gate, verify/resend +
> forgot/reset endpoints, Google auto-verified, seed backfill; migration backfills existing users
> to `email_verified=true` so nobody is locked out. Backoffice: register "check your inbox",
> verify-email/forgot-password/reset-password pages, login gate handling + resend + forgot link.
> **F15.5 UI + e2e** (`tests/e2e/notifications.e2e.test.ts`, 3 cases) also landed. **Verified:**
> typecheck, lint, 194 unit tests, `ng build`+`ng lint` all green; migration chain applies clean
> on a fresh DB. **Pending:** live stack run (rebuild in progress); real SMTP/VAPID creds; prod rollout.

> Add `notification.send` as a static RK + `NotificationSendPayload` to `packages/queue`
> (Appendix A pattern: exact binding, `DLQ_ARGS`) when F15.3 lands.

## F16 — End-to-end service testing (strategy TBD)

Full testing strategy for all services to be planned in a dedicated session. Placeholder only.

| #     | St  | Item                                                                                       | Test             | Needs |
| ----- | --- | ------------------------------------------------------------------------------------------ | ---------------- | ----- |
| F16.1 | [ ] | Define testing strategy — scope, tooling, environments, coverage targets per service layer | planning session | —     |
| F16.2 | [ ] | Implement agreed test suites across services                                               | suites green     | F16.1 |

## F17 — AI assistant (in-app copilot)

Conversational assistant inside the backoffice that helps users operate the platform:
answer "how do I…" questions, explain the user's own setup (devices, capabilities, rules,
pipelines, notifications), guide multi-step flows (provisioning, blueprint derive), and —
with explicit confirmation — act on their behalf through the existing APIs. Reuses the
ml-router → ml-executor LLM path (chat plans already route to a fast remote provider);
F11's rule generator becomes one tool the assistant can call rather than a separate AI
surface. Interactive chat is request/response over HTTP + socket streaming — not the
queue-based pipeline path (that stays for long-running ML stages per Appendix A).

| #     | St  | Item                                                                                                                                                                                                                                                             | Test                                                                                              | Needs       |
| ----- | --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ----------- |
| F17.1 | [ ] | Assistant backend: `POST /api/assistant/chat` (thin route → service), per-user conversation sessions, grounding context builder (summarize the user's devices/capabilities/rules/pipelines — same enrichment shape as F8's context builder), ml-router chat plan | curl a question about the user's own devices → grounded, correct answer                           | F8.2        |
| F17.2 | [ ] | Tool layer: read-only tools first (list devices, current sensor state, rule/pipeline status, notification explain); then guarded write tools (dispatch device action, toggle rule) — every write requires an explicit user confirmation round-trip               | assistant answers from live data; a write tool never fires without a confirm                      | F17.1       |
| F17.3 | [ ] | Backoffice chat UI: assistant panel (floating/entry in shell), streamed responses, action-confirmation dialog, conversation history                                                                                                                              | ask + get streamed answer in UI; confirm dialog gates an action; history persists across reload   | F17.1, F5.6 |
| F17.4 | [ ] | Guardrails: per-user rate limit, treat all grounded context as data (prompt-injection posture — device names/rule text never become instructions), audit log for assistant-invoked actions, provider fallback when the remote LLM is down                        | rate limit enforced; injection probe in a device name doesn't steer the model; audit rows written | F17.2       |

## F18 — History (sensors, devices & rules)

The platform currently keeps only latest state per device. This group persists and surfaces
history: sensor readings over time (including error / out-of-range / stale readings), device
online/offline (heartbeat) history — the firmware already emits heartbeat + fault read verbs
(see the unified-actions refactor), so the ingest side has a source — and rule-execution
history. Rule **fires** are already recorded (`UserRuleEvent` → `fired_at`/`triggered_value`,
exposed by `rules.service.listEvents` + `GET /api/rules/events`); the net-new for rules is
capturing **failures** (evaluation errors, action-dispatch failures) and a proper history UI.
Needs a time-series store with a retention/downsampling policy, an ingest path, a query API,
and UI (charts + availability/error/rule timelines). Builds on F13 observability but is
user-facing history, not ops telemetry.

| #     | St  | Item                                                                                                                                                                                                                                       | Test                                                                     | Needs       |
| ----- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------ | ----------- |
| F18.1 | [ ] | History schema + ingest — persist sensor readings (value, unit, quality/error flag, ts) and device online/offline heartbeat transitions to a history store; retention + downsampling policy                                                | readings + online/offline transitions accumulate rows; old rows age out  | F13.1, F3.4 |
| F18.2 | [ ] | History API — thin routes → service: query a device/capability's reading series (time range, aggregation) and its availability/error history                                                                                               | curl a device's series over a range → downsampled points, gaps, errors   | F18.1       |
| F18.3 | [ ] | UI — sensor-history charts + device online/error timeline on the device page (offline gaps + error markers)                                                                                                                                | chart renders a real series; an induced offline gap + error is visible   | F18.2, F5.6 |
| F18.4 | [ ] | Rule-execution history — record evaluation **errors** + action-dispatch **failures** alongside the existing fire events (extend `UserRuleEvent` or a sibling table); extend `listEvents`/`GET /api/rules/events` to return outcome + error | a failing rule dispatch writes an error row; API returns it with outcome | F6.3        |
| F18.5 | [ ] | UI — per-rule history timeline (fires + errors, triggered value, cooldown skips) on the rules editor / device page                                                                                                                         | a fired rule and a failed one both appear in the timeline                | F18.4, F5.6 |

## F19 — Rule dry-run / testing

The "test before it fires" capability pipelines already have (`pipeline-simulate-dialog` +
`isDryRun`/`sensorOverrides` threaded through the coordinator, F8.2), applied to rules:
evaluate a rule against supplied/overridden sensor values under the current param/phase
context, report whether it would fire and what action it would dispatch — without publishing
`ACTION_REQUESTED`. Reuses the `@lattice/params` resolver so a dry run agrees with what the
live engine would do.

| #     | St  | Item                                                                                                                                                                                      | Test                                                               | Needs       |
| ----- | --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | ----------- |
| F19.1 | [ ] | Dry-run in the rules engine — `evaluateRule` accepts sensor overrides + a dry-run flag, resolves refs via `@lattice/params`, returns would-fire + resolved action **without** dispatching | dry-run with an override → correct fire/no-fire, nothing published | F6.3, F10.1 |
| F19.2 | [ ] | API — `POST /api/rules/:id/simulate` (thin route → service) taking sensor overrides, returning the verdict + resolved action                                                              | curl simulate → verdict + resolved action; no ACTION_REQUESTED     | F19.1       |
| F19.3 | [ ] | UI — rule-simulate dialog (same shape as `pipeline-simulate-dialog`) on the rules editor                                                                                                  | enter overrides in UI → verdict shown; device-sim receives nothing | F19.2, F5.6 |

## F20 — Error readings as conditions/triggers

Today a device fault envelope (`{"error":"read_failed","action":"<name>"}` on the telemetry
topic) is deliberately **excluded** from every automation gate: `isErrorReading` short-circuits
`evaluateThreshold` (`@lattice/params`), and both the rule engine and `pipeline-triggers.ts` bail
the moment they see one — a fault is recorded to history but can never satisfy a numeric threshold.
This group makes the fault state itself a **first-class condition/trigger**, so a user can say "if
the tank-level sensor reports an error, alert me / shut off the pump" — not only "if the value
crosses N". Reuses the existing `isErrorReading` predicate as the gate (optionally matching a
specific `error` code); no new firmware or telemetry contract. Builds on F6.6 (accept + persist
error readings instead of dropping them).

| #     | St  | Item                                                                                                                                                                                                                                                                                                 | Test                                                                                                       | Needs       |
| ----- | --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ----------- |
| F20.1 | [ ] | Schema — add an `error`/`fault` condition kind to `UserRuleCondition` and an `error` trigger type to `PipelineTrigger` (reuse the existing `user_device_action_id` FK; optional `error_code` to match a specific fault, null = any error). Migration via `migrate diff` + update `prisma/SCHEMA.md`. | `prisma validate`; new condition/trigger rows persist                                                      | F6.6        |
| F20.2 | [ ] | Rules engine — `rules-logic.ts` evaluates the new error condition against the fault envelope via `isErrorReading` (matching `error_code` when set) **instead of** bailing on it; existing threshold/device_status/schedule kinds keep dropping faults unchanged.                                     | device emits a fault reading → an error-condition rule fires; a threshold rule on the same action does not | F20.1, F6.3 |
| F20.3 | [ ] | Rules UI — `rule-editor-dialog` exposes "reading is error/fault" as a condition type (capability picker + optional error-code field), with a condition summary line.                                                                                                                                 | build an error-condition rule in the browser → it fires on a simulated fault                               | F20.2, F5.6 |
| F20.4 | [ ] | Pipeline trigger — `matchPipelineTriggers` fires `error`-type triggers when `isErrorReading(value)` (moving the current early-return so fault envelopes are matched against error triggers before the value-threshold path), respecting phase scope + per-trigger cooldown.                          | a fault reading on a bound action starts a pipeline run; a normal reading does not                         | F20.1, F8.1 |
| F20.5 | [ ] | Pipeline UI — `pipeline-editor-dialog` exposes the error trigger type (same capability/error-code controls as the rules editor).                                                                                                                                                                     | add an error trigger to a pipeline in the browser → run starts on a simulated fault                        | F20.4, F5.6 |

## F21 — Dashboard tile redesign (denser tiles, readable names)

**The problem.** All three dashboard tiles (`action-card`, `group-tile`, `scene-tile`) copy the
same header row — 18px icon · 13px **single-line, `white-space: nowrap` + ellipsis** name · spacer ·
status dot / count badge · a 28px `more_vert` button — and each tile lives in a fixed-size wrapper
(`.device-card-wrapper` 216×188 → 186×178 → `calc(50% - 12px)`×200; `.scene-card-wrapper`
190×190 → 168×172 → `calc(50% - 6px)`×132) with a 14px drag rail eating the left edge. On a phone
that leaves the name roughly **100px on one line — about 8–12 characters** before it ellipsises,
while the body wastes the tile's whole middle on decorative fixed-size art (72px play badge, 56px
icon cells, 52px power button, an 88–92px capped dial) that is vertically centred with 10–20px of
padding. Derived blueprint scenes ("Tank A — Fill Cycle") and grouped device names are unreadable
exactly where the screen is smallest, and the scene-tile CSS already carries two comment blocks
apologising for the overflow this geometry causes.

**The redesign.** One shared tile shell owns the geometry; the name gets the tile's _full width_
and up to two lines; the art shrinks to fund it — with no increase in tile height on any
breakpoint. Concretely: move the name out of the crowded header row into a full-width label
(header keeps only icon + state + menu, or the menu moves off the header entirely on touch),
clamp to 2 lines with `-webkit-line-clamp` and keep the `title`/`aria-label` full text, and
replace the three hard-coded wrapper size sets with `--tile-w`/`--tile-h` custom properties on a
single `repeat(auto-fill, minmax(…))` grid so tile geometry is defined once instead of six
`@media` blocks. Purely presentational — no API, schema, or socket contract changes; drag-drop
reorder, grouping drop targets, area bands and the offline overlay must all survive intact.

| #     | St  | Item                                                                                                                                                                                                                                                                                        | Test                                                                                                       | Needs       |
| ----- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ----------- |
| F21.1 | [ ] | Tile geometry tokens — replace the duplicated wrapper sizes in `user-dashboard.css` with `--tile-w`/`--tile-h`/`--tile-gap` (set per breakpoint in one place, defaults in `styles.css` beside `--radius-card`); `.drag-placeholder` derives from the same tokens instead of restating them. | resize 1440 → 768 → 390: tiles/placeholder stay aligned; no stale hard-coded px left in the dashboard CSS  | F5.4        |
| F21.2 | [ ] | Shared tile-header/name primitive — one markup+CSS pattern used by all three tiles (`action-card`, `group-tile`, `scene-tile`) instead of three near-identical copies; name is full-width, 2-line clamped, `title` + `aria-label` carry the untruncated text.                               | a 60-char scene name shows 2 lines then ellipsis, tooltip shows all of it, tile height unchanged           | F21.1       |
| F21.3 | [ ] | Reclaim header width — collapse the status dot into the tile icon (colour/ring instead of a separate 8px dot + gap) and move the `more_vert` menu off the header row on phones (long-press → the existing bottom-sheet pattern, or a corner overlay button on the body).                    | phone: name area ≥ 2× today's character count; menu actions still reachable; keyboard/`role=button` intact | F21.2       |
| F21.4 | [ ] | Shrink the body art to fund the name — phone-tier sizes for the scene play badge, group icon cells and the power button (mirroring the existing dial `max-height` tiers), body padding/gap reduced, so the extra name line costs **no** extra tile height.                                  | 390px viewport: dial+trait-switcher card, scene tile and group tile all fit their box with no clipping     | F21.2       |
| F21.5 | [ ] | Touch drag affordance — drop the 14px `.drag-rail` on `(hover: none)` (long-press to drag via `cdkDrag` delay), returning its width to the tile content on phones.                                                                                                                          | phone: reorder still works by long-press; grouping drop hint + area-band drop targets still fire           | F21.1, F5.8 |
| F21.6 | [ ] | Verification pass — drive the dev stack in a real browser at 390 / 768 / 1440 with long device, group, derived-scene and area names; check light + dark, offline overlay, deprecated card, out-of-phase scene.                                                                              | screenshots at all three widths in both themes; no horizontal overflow, no clipped body                    | F21.2–F21.5 |

---

## F14 — Prod cutover (own full plan, scheduled by you)

Not broken down yet — gets its own plan session when you decide. Scope: prod DB reset, prod overlay switch (from gitops snapshot), device re-provisioning, DNS/ingress, Google console switch, decommission old web-app, retire old CI.

---

## Appendix A — Approved queue design (binds F1.4, F6, F8)

Full plan: `C:\Users\ishai\.claude\plans\yes-keen-petal.md` (+ flow sketch `yes-keen-petal-sketch.txt`). Approved 2026-06-05. Key decisions, so this file is self-contained:

**1. Static routing keys — no userId anywhere in RKs** (userId lives in the payload only):

```
telemetry.arrived · rules.evaluate · pipeline.trigger · pipeline.result ·
device.state.changed · action.dispatch
pipeline.stage.sensor_digest · pipeline.stage.command_exec · pipeline.stage.done.v1
pipeline.stage.{kind}.{name}.{version}        ← only dynamic part: ML model identity
```

All queue bindings are exact match (no `.*` wildcards). Shared `DLQ_ARGS` export (dead-letter exchange + 5-min TTL) applied to **every** queue assertion, static and dynamic.

**2. Async queue-based pipeline stages** (replaces the snapshot's synchronous HTTP for-loop to ml-router — that version must NOT be re-landed):

- Pipeline-worker has **4 consumers**: trigger (creates PipelineRun, publishes first stage), sensor_digest, command_exec, finalization (`q.pipeline.stage.done` → update run, publish `pipeline.result`, socket emit).
- **ml-router is a queue consumer, not an HTTP service**: `setupModelQueues()` asserts one queue per registered ML model at startup (`q.pipeline.stage.{kind}.{name}.{version}`), prefetch 1 for LLM / 4 for VLM.
- `advancePipeline()` helper (~45 lines, inlined in both pipeline-worker and ml-router): writes PipelineRunStage, then publishes next stage's queue or `pipeline.stage.done` (status completed/failed). Errors → done with `failed` → message lands in DLQ.
- Why: LLM inference can take 60+ s — HTTP blocks, no retry; per-stage queues give independent backpressure, DLQ, and per-model scaling.

**3. SensorBlueprint** (new Prisma model): `sensor_groups` JSON (named capability groups), `profile` JSON (target ranges), optional `camera_device_id`; `Pipeline.blueprint_id` FK.

- Validation **hard at pipeline creation**, trusted at runtime: any vlm/llm stage → blueprint required; any vlm stage → blueprint must have `camera_device_id`.
- `sensor_digest` stage reads groupings/profile from the blueprint (not from flat stage config); VLM/LLM handlers read the cached camera frame via the blueprint's camera device.
- Stage chains: `vlm → sensor_digest → llm → command_exec` (with camera) or `sensor_digest → llm → command_exec` (sensors only).

⚠️ The plan's JSON examples use farming terms (`plant_type`, `nutrients`…) — predates the generic-naming rule. When implementing, all examples/seeds must be domain-agnostic.

**Where it lands in the roadmap:** F1.4 builds `packages/queue` with the static RKs/DLQ_ARGS/payload types from day one (no migration later) · F6 workers consume them · F8.1 adds SensorBlueprint + validation · F8.2 the 4 consumers + advancePipeline · F8.3 ml-router model queues.

---

## Session log

| Date       | What happened                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-31 | **F21 written — dashboard tile redesign (docs only, no code).** Audit of the three dashboard tiles found one header pattern copied three times (`action-card` / `group-tile` / `scene-tile`): a single-line `nowrap`+ellipsis name squeezed between an icon, a status dot/count and a 28px `more_vert` button, inside fixed-size wrappers restated across six `@media` blocks, with a 14px drag rail off the left edge — leaving ~100px (≈8–12 chars) for the name on a phone while the tile body spends its middle on fixed 52–72px art. F21 is the presentational fix: geometry tokens (`--tile-w`/`--tile-h`) in one place, a shared full-width 2-line-clamped name primitive, header width reclaimed (dot folded into the icon, menu off-header on touch), art shrunk to pay for the name at unchanged tile height, drag rail dropped on `(hover: none)`, plus a browser verification pass at 390/768/1440 in both themes. No API/schema/socket change; drag-drop, grouping drop targets and area bands must survive.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-22 | **Keys are generated, not typed — and frozen once anyone is using them.** Review question: "why does the user choose the key? if it's internal, why not create it on the fly?" Correct, and chasing it found a data-integrity bug. A blueprint's `key` is only the handle `importBlueprint` matches on; a slot's `key` is only how templates refer to it. Both are now slugged from the human field (name / label), de-duplicated (`Water Tank!! Monitor` → `water_tank_monitor`, a second → `_2`), and shown as read-only `id …` metadata rather than being a second thing to keep in step by hand. **The bug:** `blueprint_slot_bindings.slot_key` on every _live instance_ points at the slot key, and `resolveActionIds` builds its lookup from those bindings — so a slot key that changes under a live setup makes every derived entity resolve to nothing on the next publish, silently gutting it. My auto-slug (added an hour earlier) did exactly that on any label edit. Keys are therefore frozen once `instance_count > 0`, with the reason shown inline — _"fixed, 1 live setup(s) are bound to it"_. A blueprint's own key is likewise never re-slugged after first save, or renaming it would create a second blueprint instead of updating the first. Verified headlessly: zero key inputs remain; a new blueprint self-ids from its name; two identically-labelled slots get distinct ids; a label edit with 0 live setups re-slugs _and_ rewrites 6 references (each keeping its action); the same edit with 1 live setup leaves the ids untouched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-22 | **Slot editor reworked after review feedback ("i don't understand this flow").** Three fair questions about the slot row, two of which were real defects. (1) **Field order was wrong** — key/label came before the sealed template because that is the database column order, not the order an author thinks in. The template decides which devices qualify _and_ which actions the slot can address, so it is now first and labelled "Kind of device". (2) **"Where is the action selection?"** — there isn't one per slot, and shouldn't be: a slot exposes _all_ of its template's entries and each rule/scene/pipeline row picks the one it addresses (there is nowhere in the schema to store a subset, and publish validation already checks every reference). But the row said "(2 actions)" and never showed them, so the slot now lists what it provides read-only — `Socket 1 · i2c_socket_8`, `Socket 2 · i2c_socket_8_2`. (3) **`key` and `label` were unexplained** — now "Reference id" (how rules refer to this slot) and "What to call it" (shown in the derive wizard), each with a hint, and the id auto-slugs from the label until the author types their own. **The review also surfaced a bug nobody asked about: renaming a slot key orphaned every reference to it.** Publish would catch it, but only after the author had lost the dropdown work. `commitSlotKey` now rewrites every `slot_key` across scenes/rules/pipelines and reports the count; changing a slot's sealed template clears any action the new template does not provide, rather than leaving it to fail at publish. First implementation captured the old key on `(focus)` and **silently did nothing** when the focus event did not fire — replaced with a `WeakMap` of committed keys seeded wherever slots enter the draft, so the rename cannot be missed. Verified headlessly: renaming `sockets` → `outlets` reported "6 reference(s) updated" and every reference kept its action selected. Also fixed a two-line `mat-hint` colliding with the next field's floating label (row-gap + clearance + a shorter hint; measured 12px clear, not eyeballed). `ng lint`, `ng build`, `tsc -b`, `eslint`, 264/264 green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-22 | **F10.9 rebuilt as the form builder that was actually agreed.** I had shipped a JSON document editor and written a justification for it in the comments — the plan says "Form-based admin builder", so that was me re-deciding a settled question, not a design call. Replaced with a real form: repeatable sections for details/slots/params/phases/rules/scenes/pipelines over a typed `BlueprintDraft` that is the _same shape_ the import endpoint accepts (so there is no translation layer to drift), with `toDocument`/`toDraft` stripping and restoring the empties. **The point of the form is addressing.** `action_name` must be an `mqtt_action_name` the slot's sealed template actually provides — typing that by hand is precisely how a blueprint publishes clean and resolves to nothing at derive time (the bug found on 2026-07-21). So slot and action are dropdowns, the action list is derived from the slot's chosen template, and changing a slot **clears** the action rather than letting a stale name survive; `AdminBlueprintsService.sealedTemplatesWithEntries()` fetches each released template's entries to feed them. Every value that may hold a reference gets a `{}` inserter listing the declared params, and prompt textareas get chips for `@phase.name` / `@phase.context_notes` / each param — same reasoning: at evaluation time a typo'd reference is indistinguishable from a deleted one. Publish now saves first, because publish validates what is _persisted_ and unsaved edits would otherwise be silently ignored. A "Load from JSON" panel survives as a collapsed escape hatch for the seeded verticals, feeding the form rather than replacing it. **The form immediately caught a real bug the JSON editor had merely papered over:** saving a loaded blueprint dropped `ml_model` from every infer stage, because `getBlueprint` returned `ml_model_id` and not the relation — I had listed this as an acceptable "known gap" when the editor was JSON. It is not acceptable when saving is the normal path, so `blueprintInclude` now includes `ml_model` as `(kind, name, version)` — the portable form the document uses. Verified headlessly: the form loads the real blueprint (2 phases, 3 rules, 1 scene, 1 pipeline), the tank slot's action dropdown offers exactly `Tank level (water_level)`, the `{}` picker offers all four declared params, and Save → Validate now returns **Valid** with slots, params, phase targets, references (`@phase.level.min`, `@param.pump.state`), scene members, pipeline sensors/stages/triggers and the prompt template all intact. `ng lint`, `ng build`, `tsc -b`, `eslint`, 264/264 unit tests green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-22 | **F10.6–F10.9 landed — blueprints complete (F10.0–F10.9 all `[x]`).** **Reconcile** (`blueprints.reconcile.service.ts`) matches derived entities to templates by `blueprint_key`, skips anything `user_modified`, and **never deletes** — an entity dropped from a v2 is disabled (scenes are only reported, since a scene runs on demand and an orphan is harmless). It never touches `blueprint_param_overrides`: structure is the blueprint's, values are the user's, and keeping them in disjoint tables is what makes a release non-destructive _by construction_. Runs automatically on publish (per-instance failures isolated) and on demand per instance. `user_modified` is set by the ordinary `update()` paths in rules/scenes/pipelines services — structural edits only, not enable/disable, which is not an opinion about content. **A guard had to be lifted to make any of this work:** `importBlueprint` refused when instances existed — written in F10.2 _before_ reconcile, and it blocked the primary v2 flow outright. Rewritten to keep the blueprint ROW and replace only its children (`blueprint_id` is Restrict anyway, so delete-and-recreate was never possible). That exposed a subtlety worth the trouble: `current_phase_id` is SetNull, so deleting phases would silently drop every live setup out of its lifecycle — the import now captures each instance's phase **key** beforehand and re-points after, and a phase a v2 removed leaves the instance with none rather than guessing. **F10.7**: `NotificationSendPayload` gained optional `context: { area_id, area_name }` through `@lattice/queue` types + zod (the contract lives there — the shelved branch skipped this and the feature was inert); prefixed once in `templates.render` rather than per renderer, because the area is cross-cutting and push shows the title first. **F10.8/F10.9 UI**: gallery showing readiness _before_ the user commits, a one-screen derive wizard (auto-bound slots shown filled-in and confirmable, not hidden), the instance page with phase timeline + per-param resolved value **and its source**, drift list with per-entity restore; the admin builder edits the **document** rather than a form tree, because that is genuinely what the API accepts and a field-by-field form would be a second, lossy model that quietly diverges. Verified headlessly in a real browser end to end: phase click retuned 40→20, an override showed "your value" 33, restore returned it to "from this phase" 20. **Two real UI bugs found by looking, one phantom rejected by measuring:** slot labels wrapped because a long device type squeezed them (fixed with `white-space: nowrap` on the label, letting the type ellipsise); all three pages ran flush to the viewport edges because _pages_ own their inset in this app and mine had none (now `20px 24px` + `max-width: 960px`, matching the automations shell). The third — a list row's meta line that _looked_ clipped — measured at row height 59px with meta bottom 123 vs row bottom 139, i.e. not clipped at all, just low-contrast `--text-dim` exactly like every other meta line; left alone. e2e grew to 11 cases (drift → v2 publish → reset). Full suite on the test stack: **314/316, 25/27 suites** — the same two pre-existing failures (`notifications.e2e` needs a service `compose.test.yaml` does not define; `platform.queue.integration` DLQ + teardown TypeError in untouched files). `tsc -b`, `eslint`, `ng lint`, `ng build` all clean.                                                    |
| 2026-07-22 | **F10.1 closed — resolver wired into ml-router, and a dead field brought to life.** `prompt_template` now goes through `resolveText` in `prepareLlmPrompt`, so a derived pipeline's "This loop is in its @phase.name phase. @phase.context_notes" reaches the model as actual prose instead of literal reference text (unresolvable refs are dropped **and logged**, never passed through — a raw `@phase.name` in a prompt is worse than saying nothing). **The bigger find:** `PipelineSensor.min_value`/`max_value` have been configurable since F5, loaded into `PipelinePlan`, and **never read by anything** — the user could set a normal band and nothing ever told the model. They are now resolved per run and rendered as a dedicated `Expected ranges (what "normal" means right now…)` block, which is precisely what the blueprint design wanted them for: the band is `@phase.level.min`, so what counts as normal changes with the phase and no pipeline row is rewritten. **Duplication killed before it became a third copy:** context assembly moved into `@lattice/params` as `buildParamContext` + `EMPTY_PARAM_CONTEXT`, still pure (each service does its own query and hands the rows in), now used by automation-worker, api and ml-router alike — the package's own header warns that three private copies would eventually disagree, and the api's inline `buildContext` was already the second. Context is loaded **once per plan**, so a prompt and its sensor bounds cannot straddle a phase advance mid-run. 5 new unit tests (34 in the resolver suite). Verified on the live dev stack with a dry run of the derived `Loop review` pipeline: in Steady the prompt read "…its Steady state phase… between 20% and 90%" with ranges 20/90; after `PUT /instances/1/phase` to Commissioning the same pipeline produced "…its Commissioning phase…", the commissioning notes, "between 40% and 95%" and ranges 40/95 — with `pipeline_sensors` row and `updated_at` byte-identical across both runs. Also worth noting `min_value`/`max_value` resolution is why `PipelinePlan` gained a `params` field rather than resolving lazily per stage.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-21 | **F10.4 phases landed + the first blueprints e2e suite.** Auto-advance is a **one-minute cron** in automation-worker (not the 10s rules pass — phase durations are hours at the shortest, and it keeps a near-always-idle query out of the hot path); the decision itself is pure in `phases-logic.ts` (`phaseDurationMs`/`isPhaseDue`/`nextPhase`, 15 unit tests) following the existing `rules-logic.ts` pattern, so a two-day duration is testable without waiting two days. `nextPhase` picks the **next-highest ordinal**, not `ordinal + 1`, so a blueprint numbered 10/20/30 or one whose middle phase a v2 removed still advances; a terminal phase that opts into auto-advance simply stays put rather than erroring. Advancing publishes a new `blueprint_phase_advanced` notification (renderer added to notification-service). API side: `blueprints.instances.service.ts` + routes for `GET /api/blueprints/instances[/:id]`, `PUT .../phase` (manual advance) and `PUT .../params/:key` (set/clear an override, rejecting a `user_tunable: false` param). The instance view computes each param's **resolved value and its source** (`override`/`phase`/`default`) by running the _same_ `@lattice/params` resolver the worker uses — deliberately not a second copy of the precedence, so the page cannot disagree with what the rules will act on. **First e2e suite for F10** (`blueprints.e2e.test.ts`, 8 cases): authors two sealed templates — the socket board activating the same `capability_key` **twice**, reproducing exactly the ambiguity that forced the `action_name` rename — provisions sealed sims, publishes, derives, and walks phase → default → override asserting the rule row's condition/action **ids and reference text are unchanged throughout**. That last assertion is the redesign's invariant; if it ever fails, reconcile and phase advance have started clobbering each other. Suite binds **explicitly** rather than relying on auto-bind so it is deterministic on a shared stack, and cleans up device rows with `cleanup()` (a failed run otherwise leaves sealed devices that make the next run's slots ambiguous — hit this for real). Added `apiPut`/`apiRaw` to the e2e harness (`apiRaw` doesn't throw, so "must be a 400 saying X" is expressible). **Stack lesson:** `test:e2e:up` reuses cached images — a stale `migrate` left the test DB without `users.email_verified` (login 500) and a stale `device-gateway` never materialized sealed config (derive 400, correctly naming all three unresolved actions). Rebuilding both fixed it; worth remembering that the ephemeral stack is only as fresh as its images. **Global rate limiter also made configurable** (`RATE_LIMIT_MAX_REQUESTS`, already env-driven, now actually set in the dev/test compose): a full e2e run makes far more than the strict 150-per-15-min production default from one IP, which was the real cause of the 429s — not the auth limiter alone. Full suite on the test stack: **304/308, 24/27 suites**. The 4 failures are pre-existing and unrelated — `notifications.e2e` needs `notification-service`, which `compose.test.yaml` does not define at all, and `platform.queue.integration` fails a DLQ assertion plus a teardown `TypeError` in files this change never touched.                                                                                                                                                                                                                                                                         |
| 2026-07-21 | **F10.3 derive landed + F10.1 resolver wired + a schema addressing fix.** **The fix first:** blueprint template rows addressed actions as `(slot_key, capability_key)`, which cannot name one action — a sealed template may activate the same capability many times (8 `i2c_socket_8` channels → `i2c_socket_8`, `i2c_socket_8_2`, …), and `@@unique([template_id, slot_key, capability_key])` made a 2-channel socket board **unexpressible**. Renamed to `action_name` = the entry's `mqtt_action_name` across all 5 template child models — the handle `SealedTemplateEntry` already documents as the unique one, and exactly what materialization upserts `user_device_actions` on. Migration edited in place (dev-only, unpushed) + columns/indexes renamed in the live DB + checksum re-registered. This closed the validation gap found yesterday: publish now rejects an `action_name` the slot's sealed template does not provide, listing what it does have — the old seed's bogus `i2c_socket_8` on a tank slot is now a 400, not a silent pass. **F10.3 derive** (`blueprints.derive.service.ts` + thin `blueprints.routes.ts`): preview/list-derivable match each slot's sealed-template targets against the user's sealed devices (`versionInRange`, same logic as `countMatchingDevices`), exactly-one ⇒ auto-bind, several ⇒ 400 asking for a pick, none ⇒ 400 naming the slot; derive then creates Area + instance + bindings, tags the devices, and writes scenes/rules/pipelines with `@param.`/`@phase.` refs **verbatim** plus provenance (`blueprint_instance_id`/`blueprint_key`/`user_modified`). **Two deliberate deviations from the plan:** (1) it does **not** call `materializeForUserDevice` — that lives in device-gateway, not api, and a bound sealed device was already materialized at provision time, so derive _reads_ the actions and fails naming every unresolved `(slot, action)` at once if a template was never released; (2) derived rows are written directly rather than through `rules`/`scenes`/`pipelines.service`, whose validation assumes literal values from the UI and would reject a stored reference — ownership is instead structural (every action id comes from a just-verified device). **F10.1b wired**: `param-context.ts` loads `{overrides, phaseTargets, defaults, phase}` per instance (one query per pass, shared across that instance's rules); `rules.engine` resolves `threshold_value` and `target_state` and **fails closed with a warning** on an unresolvable ref. Verified on the live stack with two sealed sim devices: derive auto-bound both slots, the derived rule fired (`target: "ON"` — `@param.pump.state` resolved), and **advancing the phase in one column flipped the same rule from firing to not firing with `updated_at` and `threshold_value` byte-identical** — the central mechanism, demonstrated. Dev data had no usable sealed templates (`farm_controller` had zero entries), so two real ones were composed and released via the API. **Auth rate limiter made configurable** at the user's request (`AUTH_RATE_LIMIT_*`, strict 10/15min default preserved; raised only in `compose.dev.yaml`/`compose.test.yaml`) — an e2e run legitimately logs in far more than 10 times from one IP. `npx tsc -b`, `eslint`, 244/244 unit tests green. **e2e cannot run while the dev stack is up**: both compose files publish api on 3100 and the harness reads `.env.test` (admin/admin), so it authenticates against the dev stack with test credentials and 401s — pre-existing, unrelated to this change. |
| 2026-07-21 | **F10.1 params library + F10.2 blueprint schema landed.** **`packages/params`** (new shared lib): `resolveParam` (whole-value fields), `resolveText` (references embedded in prompt text), `validateParamRefs`/`validateParamKey` (the "validated on write" half), precedence override → phase → default, 29 unit tests registered in `docs/TEST-PLAN.md`. Two things the design doc implied but didn't pin: `@phase.name`/`key`/`context_notes` are **reserved** phase metadata (not overridable; a param may not claim those keys) and unresolvable refs inside free text are **dropped and reported** rather than left verbatim in an LLM prompt. Resolver is **sync** (§3 sketched `await`, but ctx is loaded once per entity so there is nothing to await). **Not yet wired** into `rules.engine`/ml-router — that needs a context loader, which needs F10.3, so F10.1 stays `[~]`. **F10.2 schema:** 17 tables (`blueprints` → slots/params/phases/phase_targets + scene/rule/pipeline template trees + `blueprint_instances`/slot_bindings/param_overrides), provenance columns (`blueprint_instance_id`, `blueprint_key`, `user_modified`) on `user_rules`/`scenes`/`pipelines`, migration `20260721120000_blueprints` hand-written again (`prisma migrate diff` silently returns an empty diff for every input on 7.8.0 — the tooling is broken here, not just `migrate dev`), applied via psql and back-registered with its sha256; `.gitattributes` already pins `*.sql eol=lf` so the checksum survives checkout. ERD + tier-7 section + worked resolution example in `prisma/SCHEMA.md` (29→46 tables). **Authoring surface is `POST /api/admin/blueprints/import`** (the F12.1 document shape) since the builder UI is last; slots and ML models are referenced **by name**, not row id, so a document ports between databases. Publish is the gate. Verified against the live stack: valid vertical imports and publishes 200; a copy with a typo'd slot and two typo'd refs is refused 400 listing all 7 problems — including the ref embedded in `prompt_template`, which only `findParamRefs` catches. `exception.middleware` now passes a `details[]` array through (same precedent as the existing `email` passthrough) so the caller gets every reason in one response. Seeded vertical is deliberately **generically named** (`monitored_tank`) per the hard rule — domain wording is admin-typed runtime content only. Verification needed sealed templates released; releasing via the API re-applies config to live devices, so status was flipped in SQL and **restored to `draft` afterwards** — the probe blueprint was deleted too. `services/api/Dockerfile` gained the params lib (build failed without it).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-21 | **F10.0 Areas landed end-to-end → `[x]`** (branch `feat/blueprints-areas`, commits `3a50095` + `a4b57f5`). Schema: `Area` model (`user_id`, `name`, `sort_order`, `@@unique([user_id, name])`) + `area_id Int?` on `UserDevice`/`Scene`/`UserRule`/`Pipeline`, all `onDelete: SetNull` so deleting an area never deletes anything; migration `20260720130000_areas` **hand-written** (the repo's `migrate dev`/reset path is broken) and applied via `psql --single-transaction`, then recorded in `_prisma_migrations` with its real sha256 so `migrate deploy` won't re-apply; `prisma/SCHEMA.md` ERD updated (28→29 tables). API: `areas.service.ts` (list with `_count.devices`, create/rename/reorder/assign/delete, `ensureOwned`) + thin `areas.routes.ts` (`/assign` + `/order` declared before `/:id`); `area_id` exposed on `DeviceView` and `areaId`/`areaName` on `ActionView`. Backoffice: `AreasService`, area-assign dialog (pick / create inline / none), **area-manage dialog** (CDK drag-to-reorder + rename + delete), and the dashboard rebuilt as **filter chips + area bands + drag-a-card-onto-a-band to reassign** — all three sketches (A+B+C) combined at the user's request. The dashboard's fragile custom drag hit-testing was preserved by keeping **one** `cdkDropList` with global indices and adding band hit-testing beside the existing card hit-testing (`drop()` precedence: card-onto-card group drop → band drop → reorder). Action cards restyled compact per user. Verified in a real browser (headless Edge + CDP) at desktop / tablet / 425px / 390px. **Two sizing lessons:** (1) in `user-dashboard.css` the phone/tablet media blocks sit **above** the base rules, so an equal-specificity override silently loses — the caps are written as `.card-content .speed-dial` on purpose (comments left in place); (2) `overflow: hidden` makes `scrollHeight == clientHeight`, so a "does it overflow?" check built on that always reports zero — it hid a real dimmer-dial overflow on desktop (18px) and tablet (23px). Overflow is now checked by measuring child rects against the container. Working tree also carries an in-flight, unrelated **F7.1 Google-auth login move** to the backoffice (see the F7 table).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-07-20 | **F10 Blueprints redesigned; F10.5 Scenes accepted; Areas (F10.0) build started.** The 2026-07-10 F10 design was built, disliked, and **shelved** on `wip/blueprints-and-ai-rulegen-shelved-2026-07-13` (as-built post-mortem + 8 findings in `docs/plans/blueprints.md`). Replanned from the problem → `docs/plans/blueprints-redesign.md`. Decisions with the user: **slots bind through released `SealedTemplate`s** (reuse version-range match + `materializeForUserDevice`; no `DEVICE_ROLE_STR`/`role_tag`/firmware change; non-sealed slots out of scope); **reconcile not snapshot** (blueprint = desired state, releases flow structure into live instances, skip `user_modified`, never delete/never touch overrides); **parameterize not mutate** (derived automations store `@param.`/`@phase.` refs resolved at eval time — user override → phase → default — via shared `packages/params` in automation-worker + api + ml-router; phase advance = 1 column write); **Areas** as a standalone user-createable primitive powering dashboard sectioning + notification context + AI scope (a derive makes one); **phases auto-advance** on elapsed duration (rides the 10s cron). F10 table rebuilt (F10.0 Areas → F10.9 builder-UI-last); pipeline params committed to v1 (resolver runs in ml-router `enrich`/`buildPrompt` too). **F10.5 → `[x]`** (standalone Scenes on `master`, `df16454`). Started building **F10.0 Areas** on branch `feat/blueprints-areas`. Also: F11's `ai-rule-generator.md` link was dangling (never committed) — reworded to point at the in-roadmap spec.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-07-10 | **F10 + F11 design plans written (docs only, no code).** Two new design docs: `docs/plans/blueprints.md` (F10) and `docs/plans/ai-rule-generator.md` (F11), both linked from their roadmap sections. **F10 decisions resolved with the user:** per-slot dual matching (semantic `capability_key` OR exact device pin), derive = snapshot + recorded version, sealed devices via firmware `DEVICE_ROLE_STR` build flag riding the manifest→seed-catalog path (no provisioning-protocol change), Scenes as a standalone feature (F10.5, prerequisite for derive), grouped notifications via optional instance context on `NOTIFICATION_SEND`, generic lifecycle phases, dashboard context-grouping contract now / shaped skins later, and generic-engine/domain-data naming (domain blueprints are runtime admin content only). **F11 redesigned chat-first** (supersedes the one-shot `POST /api/ai/rules/generate` sketch): `rule-builder` chat mode filling the existing ml-router stubs (NoopEnricher → context-provider interface; `response.processor.ts` → suggestion extraction), new `AutomationSuggestion` table (raw DTO JSON, revision chain, never evaluated), approval materializes through existing validated create services, **user-selectable builder model** (allowlist-validated `ChatIntentPayload.model` override + `GET /api/ml-models` picker), blueprint context added post-F10 as one more provider. F10/F11 tables extended accordingly (F10.5–F10.7, F11.1–F11.5).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-10 | **F6.7 camera last-frame on load → `[R]` (uncommitted).** New `GET /api/actions/:id/last-frame` in `api` (owner-scoped, gated to `CameraAction` impl types) returns the newest **non-error** `sensor_history` row as `{frame, capturedAt}`, or **204** when none yet. Chose `sensor_history` over the digest `camera_frame:{deviceId}` Valkey cache deliberately — the cache is keyed per **device** (lossy for a device with >1 camera) whereas history is per **action**, and `api` needs no ioredis wiring; the `@@index([user_device_action_id, recorded_at])` backs the `ORDER BY recorded_at DESC LIMIT 1`. **Read-only** — no per-frame `current_state` writes reintroduced. Frontend: `UserActionsService.getLastFrame()` + a backfill in `CameraDisplayComponent.ngOnInit` (the shared render point for dashboard cards + group sheet) that sets `action.state` only if a live socket frame hasn't already, with a second in-flight guard in the callback so a fresher live frame can't be clobbered. Verified: `tsc -b` clean, `ng build` clean (only the pre-existing device-config NG8107 warning). Live end-to-end (idle camera paints last frame on load) still to run against a stack with real frames. Same-day: **F6.6 confirmed already done** (6b fault telemetry persists error readings to `sensor_history`, history-only by design); **F7.7 confirmed done** (per-env sealed `google-auth-secret`, separate staging+prod files) pending confirmation the staging Google client differs from prod.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-08 | **Firmware refactor Phases 6.0/6d/6e + RSSI landed (uncommitted, all verified green).** **6.0 unified action model:** new `DeviceAction` base — `BaseCommandAction`/`BaseTelemetryAction` both extend it (command + read surfaces); one `std::vector<DeviceAction*>` + verb dispatch; `tick()` replaces the split telemetry/command loops; camera `take_picture`=`read`. Manifests byte-identical (zero metadata drift). **6e read verb:** command action reports NVS state on ack (pre-validation intercept). **6d behavior-config catalog:** `capability_configurations` + `user_action_configurations` tables (behaviors = rows: command\|interval\|on_demand; **typed columns** `min_interval_ms`/`interval_ms`/`camera_*`, not JSON — matches the de-JSON convention) + migration `20260708120000` + SCHEMA.md ERD; firmware serializer emits `configurations`; seed-catalog ingests (append-only, matching pins/traits); device-gateway serves resolved `behaviors`; firmware+sim gate each surface (interval/on_demand/command); api `PUT /api/actions/:id/behaviors` validates against catalog + rewrites selections; backoffice device-config renders per-behavior toggles. **RSSI on devices page:** heartbeat rssi persisted to `user_devices.rssi`/`last_heartbeat_at` (migration `20260708130000`) by the digest heartbeat consumer, exposed by `GET /api/devices` (nulled when offline), shown as a strength-colored signal row. Verified: 6 fw envs build, native 12/12, cppcheck clean, manifests, `tsc -b`/`eslint .`/62 unit tests, `ng build`. **Phase 7 NimBLE deferred** — the size ceiling is already solved (mini-prod 86%, ~272 KB headroom from the logging migration), and a NimBLE swap needs an on-hardware provisioning test before it ships; the CI size gate + `LOG_LEVEL=INFO` quick-wins are in place.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-08 | **Firmware refactor Phase 6b + 6c landed (uncommitted).** **6c MQTT heartbeat:** device pings `.../heartbeat` every 60s (`{uptimeMs,freeHeap,rssi,version}`, `mqtt.h` publishHeartbeat / `main.cpp` handleHeartbeat); `acl.conf` publish rule added; `@lattice/queue` gained `RK.DEVICE_HEARTBEAT` + queue/binding + `deviceHeartbeatSchema`; mqtt-service `device-heartbeat.handler.ts` forwards it; digest `device-heartbeat.consumer.ts` writes `device_last_seen:<id>` (200s TTL); sim heartbeat loop + PARITY row; unit (queue-contract) + `heartbeat.e2e.test.ts`. **6b fault telemetry:** a failed sensor read publishes `{"error":"read_failed","action":"<name>"}` on the normal telemetry topic (`BaseTelemetryAction::execute`); digest `telemetry.consumer` branches on `isErrorReading` (new guard in `threshold.ts`) → records to `sensor_history` (timestamped for duration queries), **skips** current_state + rules + pipeline triggers; sim `failTelemetry`/`dev.faults` knob + PARITY row; unit (threshold guard) + `telemetry-fault.e2e.test.ts`. **Roadmap — `sensor_error_duration` trigger:** timestamped error rows enable a future trigger type that fires when a sensor errors continuously beyond a threshold duration (e.g. power-supply temp sensor errors >5 min → overheating risk → automation). Design deferred, storage shape now in place. Prod EMQX ACL in `oci-k3s-gitops` needs the same heartbeat rule (deferred — ask before touching that repo).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-03 | **F8.2 correction.** The same-day wider audit below wrongly marked F8.2 as only half-built (claimed `sensor_digest`/`command_exec`/`finalization` consumers didn't exist) — user had just tested pipelines end-to-end and pushed back. Re-checked directly: the earlier pass grepped for literal consumer/file names and missed `services/ml-router/src/pipeline/stages/{enrich,command-exec}.ts` and the `finish()` function inside `coordinator.ts`. All 4 stages are real, just shaped as one coordinator process (enrich + command_exec + finalization run in-process; only ML infer goes to a queue) rather than 4 separate queue consumers — a valid, arguably better implementation of the same design intent. F8.2 → `[x]`. Lesson: a subagent code audit that reports "X doesn't exist" based on a name/pattern search needs verification before being trusted, especially against user-observed behavior.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-07-03 | **Wider status reconciliation (F6.3, F7.1, F8.1 → `[x]`; F8.2 → `[~]`; F9.1 → `[~]`).** Following the F5 audit (below), checked whether other roadmap gaps were actually just undocumented: `services/api` already has full CRUD for both rules (`rules.routes.ts`/`rules.service.ts`, incl. `is_emergency` + emergency event filter) and pipelines (`pipelines.routes.ts`/`pipelines.service.ts`/`pipelines-runs.service.ts`), both wired to matching backoffice UI (`rule-editor-dialog`, `pipelines`/`pipeline-editor-dialog`/`pipeline-run-history`/`pipeline-simulate-dialog`); `google-home` already has the complete OAuth account-linking flow (`google.auth.routes.ts`: `/auth`, `/auth/login`, `/token`). Pipeline validation took a different (still valid) shape than planned — code-based off `DeviceCapability.implementation_type` instead of a separate `SensorBlueprint` table; F10's blueprint tier is unaffected. Two real gaps surfaced: **F8.2** only has the trigger consumer + `advancePipeline` (ml-router/ml-executor) — `sensor_digest`/`command_exec`/`finalization` consumers don't exist, so no pipeline can finish a run yet. **F9.1**'s CRUD half was absorbed into F6.3 (emergency merged into rules at F1.7); only the device-side fast-path/cache evaluation is still missing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-07-03 | **F5 status reconciled via code audit (F5.1–F5.6/F5.8 → `[x]`; F5.9/F5.10 → backlog).** The roadmap had never been updated after this UI landed on `master` earlier, so it still showed all of F5 as `[ ]`. Audited the actual `backoffice/` code against each F5 item: theme (dual palette + localStorage), auth/login (Google popup + JWT decode), socket service (`action_state_update$` + status observables), dashboard/groups/bottom-sheet, device mgmt + device-config, routing/nav, and mobile responsiveness are all implemented and confirmed pointed at the new v2.2 services (`api`, `device-gateway`, `socket-server` — no leftover monolith routes). F5.9 (admin users page) and F5.10 (user preferences page) genuinely don't exist — moved to backlog (user call: not required for F5 close-out). F5.7 (CI/staging deploy) stays the only open F5 item.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

| 2026-07-01 → 07-03 | **Unplanned/backlog work landed on `master` (not tied to an F-numbered item), plus CI/staging verification.** (1) **Backoffice UI:** registration + legal pages added with a redesigned theme, dashboard refactor, device-management page + device-config page updates, "last online" surfaced on actions + a delete-device dialog, `DeviceSocketService` simplified (local-variable cleanup), lint hooks added and existing lint errors cleaned up, and `backoffice-lint` hook now runs a TypeScript compile check (not just ESLint). None of this was cross-checked against the specific F5.x acceptance criteria in this pass, so no F5 status flags were changed — treat as UI progress toward F5, not a completed item. (2) **New `tools/device-sim`** (not `services/`): standalone CLI ESP32 simulator (HTTP+MQTT+WS) with fleet-config support (`fleet.dev/example/test.json`) for running multiple simulated devices at once, JPEG frame generation with a timestamp overlay (jimp) for camera actions, and on-demand capture over HTTP/WS. Distinct from the F4.2 in-process `SimDevice` test fixture used by `tests/e2e/` — this is a separate manual/CLI tool, not yet wired into the Jest harness. (3) **F15.0 pre-work confirmed landed in code**: `digest-service`'s `ota-incoming.consumer.ts` now publishes `q.notification.publish` on OTA events (the note further down in the F15 section already described this; this is the commit that landed it). (4) `ml-executor`'s queue setup fixed to handle a missing prompt on the llm stage. (5) Firmware version bump to `v2.0.180` (no functional change beyond the version string). **CI/staging check (2026-07-03):** all 13 GH Actions workflows green on `master` HEAD; Kargo staging auto-promoted everywhere except `ota-staging` (see position note above — errored on git-push with a DNS resolution failure, freight for `v2.0.177` stuck, needs retry). |
| 2026-07-02 | **F3.7 static code review (Wokwi ruled out — failed twice, unreliable BLE/WiFi emulation; real hardware deferred to staging).** Reviewed `main.cpp` boot/provisioning flow, `ProvisioningBleService.h`, `JwtService.h`, `PreferencesManagerService.h`, and the wire models against the live device-gateway contract already verified in F3.2/F3.3 — all fields match. **Found + fixed a real bug**: `PreferencesManagerService::LoadActionState()` returned a dangling pointer into a destroyed temporary `String` (UB) and leaked an NVS handle on every successful call — hit on every boot for every command action via `BaseCommandAction::loadState()`. Fixed to return `String` by value; rebuilt clean. Noted but left alone: dead efuse-derived `deviceID` in `ProvisioningBleService`, and BLE payload write has no chunk-reassembly (single-write assumption, depends on mobile app MTU negotiation — mobile client not in this repo). F3.7 stays `[~]`; on-device BLE/NVS/WiFi behavior needs real hardware against staging. |
| 2026-07-02 | **F3.2/F3.3/F3.4/F3.6 verified against the live local compose stack (all → `[x]`).** F3.4 (mqtt-service): confirmed continuous device-sim telemetry flowing end-to-end (device → EMQX → mqtt-service → `q.telemetry.arrived` → digest state writes), and a manually published `action.dispatch` message routed through mqtt-service → MQTT `command/outlet` topic → device-sim received + acked → ack routed back → `q.action.result` → digest wrote authoritative state; all 4 static queues present with active consumers. F3.2 (provisioning): full curl E2E — login → provision-token → `/provision` → permanent device token → `/refresh-token` — all green, upsert-by-mac idempotent. F3.3 (device config): **found and fixed a real bug** — `device-configuration.routes.ts` read `req.device?.deviceId`, but the device_usage JWT is minted with `{userid, clientid}` (EMQX-compatible naming, per F3.2), so `deviceId` never existed on the decoded payload and `GET /config` returned `400 Missing deviceId` for every legitimately provisioned device, 100% of the time — `camera.routes.ts` already had the correct `{userid, clientid}` pattern; applied the same fix, rebuilt + restarted device-gateway, re-verified `200`. F3.6 (camera intake): POSTed a JPEG to `/api/camera/frame` with a device token → `200`, confirmed it reached digest-service via `q.telemetry.arrived` and was correctly DLQ'd (unactivated test action — expected). **F3.8 (OTA) could not be verified locally** — `services/ota-manager/firmware/` is empty in this dev environment (no `.bin`/`latest.json`), so there's nothing for ota-manager to announce; matches the roadmap's existing note that on-device OTA verification is still pending. Next: F3.7 firmware review. |
| 2026-07-02 | **`backend/` monolith deleted entirely (Backend Teardown Tracker → complete).** User call: "prod doesn't matter right now" — relaxed the F14-gate for this one change. Ported the 3 remaining pieces first: Google OAuth account-linking was already fully built in `google-home` (just needed an ingress route); added device lifecycle commands (reprovision/soft-reset/hard-reset/restart) and admin-catalog actions-list + set-default-trait to `api`. Then deleted `backend/`, `Dockerfile.backend`, `server-ci.yml`, and the related VSCode debug configs/workspace entry; repointed backoffice off `environment.apiUrl` (field removed) and deleted 2 dead methods it exposed. Gitops: deleted `k8s/base/web-app`; staging **and prod** overlays now route `/api` → `api` and `/api/google` → `google-home` — prod never had an `api` deployment, so added `../../base/api` + image pin + `ALLOWED_ORIGINS` patch (also fixed a pre-existing gap: `k8s/base/api` had no `RABBITMQ_URL`, needed by pipeline triggers and the new device commands). Kargo `app-staging`/`app-prod` now ship migrations only (`lattice-server` dropped from the `lattice-app` Warehouse and both promotion steps); `api-prod` stays manual like every other `*-prod` stage. Both overlays verified with `kubectl kustomize`. **Not pushed** — this is staged locally; pushing to gitops `main` is what actually triggers ArgoCD to prune the old `web-app` deployment from prod. |
| 2026-06-30 | **TODO: Make GitHub repository private.** Lattice repo is currently public; switch to private before shipping any prod credentials, device keys, or sealed-secret tooling instructions that reference internal hostnames. Do via GitHub repo Settings → Danger Zone → Change visibility → Private. |
| 2026-06-26 | **Schema review revisions (pre-push).** From user review of `prisma/SCHEMA.md`: (1) **De-JSON'd stable-shape data** — `user_device_actions.pins` JSON → child table `user_device_action_pins {key, pin_number}` (mode from catalog slot, joined by key); `user_rule_conditions.parameters` JSON → typed columns (`operator`, `threshold_value`, `user_device_id`+`status_value`, `schedule_time`, `schedule_days Int[]`), mirroring `pipeline_triggers`. JSON kept only for ML audit blobs + model/Google metadata. (2) **Merged emergency into rules** — dropped `emergency_rules`/`emergency_events`; added `user_rules.is_emergency` + generic `user_rule_events` fire log (an emergency = a threshold rule with `is_emergency=true`). (3) **Tiered ordering** 0→6 in both `schema.prisma` and `SCHEMA.md`. (4) Added action ordering note (`sort_order` on actions+groups, `ordinal` on stages — already existed). Updated services: automation-worker `rules.engine.ts` (typed conditions), device-gateway `action-migration`/`device-configuration` (pins relation). Regenerated client + single `0_init` (25 tables, no drift); all 4 services typecheck clean. **Rule saved to memory:** any schema change must update `prisma/SCHEMA.md`. Still local, not pushed. |
| 2026-06-26 | **F1.8 init migration consolidation + service reconciliation (F1.8 → [x]).** Squashed the 20 old migrations into a single `0_init` (generated via `prisma migrate diff --from-empty --to-schema`; `migrate dev`/`reset` unusable because they run the broken seed and this Prisma v7 build has no `--skip-seed`). Verified on a wiped local DB: deploy applies, `migrate status` clean, DB↔schema diff empty. No baselining (prod data expendable, reset at F14). Regenerated `@lattice/prisma-client`; regenerating it surfaced that **4 already-built services lagged the F1.5/F1.6 renames** — fixed digest-service (`resolve.ts`), automation-worker + google-home (DALs/services: `DeviceAction`→`DeviceCapability`, `action_id`→`capability_id`, `group_name`→`group.name`, `ActionTypeTrait`→`DeviceCapabilityTrait`), device-gateway (`action-migration` lost the separate `DeviceAction` template upsert — now points `capability_id` at the new version's `DeviceCapability`; pins from `DeviceCapabilityPin[]`; `device-configuration` `action`→`capability`). All 4 typecheck clean. gitops `migration-job.yaml`: dropped the `|| migrate resolve --applied <old init>` drift hack. Wrote `prisma/SCHEMA.md` (mermaid ERD + per-table examples) for review. **Changes staged locally, not pushed** — awaiting user review. Seeds deferred to F1.9. |
| 2026-06-26 | **F1.7 schema review accepted (F1.5–F1.7 complete; position → F1.8).** Rules: `UserRule` timestamps non-nullable; `UserRuleCondition` + optional `user_device_action_id` FK. **ML unified** — deleted `DeviceModelConfig` + `ModelAnalysisLog` (F1.6 ML tables); single-model analysis is now a 1-stage pipeline. **Concrete Pipeline tier** added (6 tables, user-owned, bound to `UserDeviceAction`): `Pipeline`, `PipelineSensor`, `PipelineStage` (kind=infer/sensor_digest/command_exec; infer→`MlModel`; command_exec value from LLM structured output), `PipelineTrigger` (telemetry/schedule/manual), `PipelineRun`, `PipelineRunStage` (audit trail). Ripple: `User.pipelines`, `MlModel.pipeline_stages` (was model_configs), `UserDeviceAction` +rule_conditions/pipeline_sensors/pipeline_exec/pipeline_triggers, −model_config. **Abstract capability-based `PipelineBlueprint` + derive deferred to F10** (build abstraction with its consumer; portability FK-vs-key + snapshot-vs-live resolved there; extend to rules+emergency). Both schema files (`prisma/` + `packages/prisma-client/`) `prisma validate` clean. No migration (F1.8). |
| 2026-06-25 | **Backoffice UI redesign — Device Config + theming fixes (master).** (1) **Device Config page full glassmorphism redesign:** replaced legacy `mat-card` + hard-coded hex with CSS-variable glass layout. Left rail uses `--nav-bg` backdrop blur; device header card shows online status dot + icon; action/blueprint cards use `.lattice-card` with primary-color glow on expanded, amber `--warning` on deprecated, green `--online` left border on active. Type chips: sensor = `--stage-vlm` violet, command = `--primary`. (2) **Mobile layout:** on ≤599px the left device rail hides and a horizontal sticky chip selector appears at top; `dc-layout` becomes the scroll container (`height: calc(100dvh - var(--bottom-bar-height)); overflow-y: auto`) so all cards are reachable above the bottom bar. (3) **Bottom tab bar:** Device Config (`/device-config`) added as the 4th mobile tab (replaced Rules, which stays on desktop/tablet sidebar). (4) **Designer feedback applied:** `impl-type` span (implementation_type) and `mqtt-name` span removed from all card subtitles — too technical for user-facing UI. (5) **Angular Material theme fixed:** `custom-theme.scss` was locked to a single `mat.$violet-palette` causing all Material components (buttons, inputs, progress bars) to be purple regardless of dark/light mode. Updated to dual-theme: azure palette (light) + cyan palette (dark), matching CSS vars `--primary: #4f46e5` → `#38bdf8` on toggle. (6) **Devices page:** Restart / Soft Reset / Hard Reset / Update Device menu items now `[disabled]="!device.online"` — offline devices can still be renamed or deleted. |
| 2026-06-24 | **F8.4 accepted.** VLM handler (`OnnxVlmProvider`: YOLO11-nano ONNX, 640×640, NMS, classes `immature`/`mature`/`damaged`) + LLM handler (`OllamaProviderService`: `qwen2.5vl:7b`, streaming) both implemented in `ml-executor`. Model file placed at `models/yolo/v1/yolo.onnx`. Ollama running on cluster via tunnel. Class names in `models.json` updated from placeholders. Both handlers wired to generic Redis (`infer:jobs`/`infer:response`) and RabbitMQ model queues. Inference E2E testing deferred to F16. |
| 2026-06-24 | **ML orchestrator layer implemented and committed.** Renamed `services/ml-router` → `services/ml-executor` (`@lattice/ml-executor`); CI `ml-router-ci.yml` → `ml-executor-ci.yml` (image `lattice-ml-executor`). New `services/ml-router` orchestrator (port 3009, `@lattice/ml-router`): `ChatIntentPayload`/`chat:intent` contract; generic `InferJobPayload`/`infer:jobs`/`infer:response` Redis entrypoint on executor; merged `handlers/chat.handler.ts` (psubscribe `chat:intent`, publish `infer:jobs`, relay via `infer:response:<id>` → `chat:response:<id>`); RMQ pipeline coordinator (`pipeline/coordinator.ts`) consumes `PIPELINE_TRIGGER`+`PIPELINE_STAGE_DONE`, dispatches stages to executor. Gitops: `k8s/base/ml-executor`+`k8s/base/ml-router` (NEW); staging overlay has both; prod overlay has executor only; Kargo warehouse+stages for both; `ml-router-ci.yml` (image `lattice-ml-router`). |
| 2026-06-24 | **F7.8 google-home validated (F7.2–F7.8 → [x]; teardown tracker → 🟢).** `docker compose build google-home` green; `GET /health` 200; backend build clean without smarthome routes; `actions-on-google`+`googleapis` already absent from `backend/package.json`; Traefik staging ingress rule `/api/google/smarthome` → google-home service confirmed present. |
| 2026-06-17 | **F7 google-home service extracted from backend monolith (F7.2–F7.6 → [R]).** New `services/google-home` (port 3010) owns SYNC/QUERY/EXECUTE Smart Home webhook + HomeGraph reporting. `google.smarthome.routes.ts` + `services/google-smart-home/` **deleted** from backend; registration removed from `backend/index.ts`. EXECUTE dispatch: replaced `actionHubService.dispatch()` with direct `publish(ch, RK.ACTION_DISPATCH, payload)` via `@lattice/queue`. HomeGraph now **enabled** (`willReportState: true`): `action-result.consumer` consumes `q.action.result`, resolves `deviceId+actionName → action`, calls `homegraph.devices.reportStateAndNotification` best-effort. Infra: multi-stage Dockerfile, `k8s/base/google-home`, staging+prod kustomize overlays, Kargo warehouse+stages (auto-promote staging, manual prod), `compose.yaml`, VSCode launch+tasks. **Pending (F7.8):** validate build/health check, audit `backend/package.json` (`actions-on-google`/`googleapis` removal), add Traefik staging ingress rule for `/api/google/smarthome`. |
| 2026-06-17 | **Monolith ws + MQTT stripped; UI repointed to socket-server (F2.7 → 🟢, F3.4 → 🟢).** (1) **Inbound control redesigned:** the UI only knows the UserDeviceAction id, and the device command body must be `{value,duration}` — so socket-server inbound now publishes `action.requested {userId,actionId,value,duration}` (replacing the earlier `q.action.dispatch.state` fan-out, which wrongly treated `command`==state). New `packages/queue` RK/queue `action.requested`. digest `action-requested.consumer` resolves actionId → `{user_device_id, mqtt_action_name, device.version}`, writes optimistic `current_state` + cache + echo, then publishes `action.dispatch` for mqtt-service → device. (2) **socket-server JWT fix:** monolith app_usage tokens carry the user id as **`id`** (not `userId`) — reads `decoded.id ?? decoded.userId`; rooms now match digest's `user_{id}`. (3) **UI:** added `environment.socketUrl` (prod `''` same-origin, dev `http://localhost:3007`); `device.socket.service` connects there; aligned `device_status_change` field `state`→`online` in dashboard/mgmt-list/group-sheet; `publishActionState` unchanged (`{actionId,state}`). (4) **Monolith ws strip:** deleted `socket.service.ts` + `socket.actions.service.ts`; removed emit call sites in index/mqtt/action.hub/emergency/vlm (emergency_alert/vlm_error left as F9/F8 TODOs). (5) **Monolith MQTT strip:** deleted `mqtt.service.ts`; new `command.dispatch.service.ts` publishes `ActionDispatchPayload` to `q.action.dispatch` (lazy `@lattice/queue` channel, resolves device version); rewired 6 publish sites (action.hub + 4 device-mgmt command routes + delete-device); added `@lattice/queue` + `rabbitmqUrl`, dropped `mqtt`/`socket.io`/`ws` deps. **Known regressions until re-land:** Google HomeGraph telemetry reportState (F7.5), emergency_alert (F9), vlm_error (F8/F11) no longer emitted from the monolith. Verified: all builds green (libs/digest/socket-server/backend typecheck/backoffice); runtime — monolith-shape `id` token connects, outbound room delivery, inbound → `q.action.requested`. |
| 2026-06-12 | Stage 0 done: v2.2 parked on `v2.2-snapshot` branches (app `01177fb`, gitops `c2d5af2`), master/main restored & verified, `v2.2` integration branch pushed, roadmap written. |
| 2026-06-12 | CI separation: `server-ci.yml` no longer triggers on backoffice changes. `backoffice.yml` now triggers on both `master` + `v2.2`, removed premature prod-overlay deploy step (re-added in F5.7). VSCode debug: added "Debug Backend (dev + hot reload)" nodemon+inspect config + "Full Stack Debug (dev)" compound. |
| 2026-06-15 | **Device Config + Dashboard overhaul (on master).** (1) Fixed VSCode debug: port-9229 kill task + correct `TS_NODE_PROJECT` path. (2) Fixed MQTT "Not Authorized": seed now `DO UPDATE` the password hash instead of `DO NOTHING`. (3) Device Config page opened to all users (`/device-config`); admin template management moved to `/admin/templates`. (4) Blueprint activation: users can activate/deactivate capabilities from `device_capability_blueprints`; UI shows pin assignment fields per slot. (5) Edit active actions: inline expand to rename, change pins, change interval; auto-sends restart command on any action change. (6) Dashboard fixed for all action types: `activateBlueprintAction` now copies `google_type_id` and inserts `action_type_traits` from the blueprint — dashboard uses `hasTrait()` / `googleType` only (no `implTypeOf` workaround). `iconForAction()` falls back to `implementation_type` for the card icon. Backfilled existing `device_actions` and `action_type_traits` for previously activated blueprints. (7) ESP32 firmware: added `pins` field to `CapabilityDescriptor`; `ProvisionRequest.toJson()` now serializes `configurable_pins` as `[{key,label,mode}]` from each action's `blueprint()`; all 11 action classes updated. (8) Backend `BlueprintView` now carries `configurable_pins` from DB; `PinSlot` type replaces hardcoded `pin-blueprints.ts`. Backfilled existing blueprint rows in DB. (9) Removed dead code: `device-configure-dialog`, `pin-blueprints.ts`, "Configure Actions" device menu item. |
| 2026-06-16 | F1.1/F1.2/F1.3/F13.2 marked done (all landed on master/main in prior sessions). Infra pass: RabbitMQ + Valkey (replaces Redis, drop-in) added to `compose.yaml` and k8s staging bases (`k8s/base/rabbitmq/`, `k8s/base/valkey/`); staging overlay wired. F1.4 `packages/queue` implemented: static RKs (no userId), DLQ_ARGS, all static queues + exact bindings, typed payloads, `connect()`/`publish()`/`consume()` helpers. |
| 2026-06-16 | Valkey k8s rename complete. `k8s/base/redis/` → `k8s/base/valkey/`; all resource names, Service hostname, volume, and secret refs updated to `valkey`/`valkey-secret`. Both overlays (prod + staging) wired. All SealedSecrets re-sealed (key rotation included). Merged to `oci-k3s-gitops` `main` (commit `21cdcb2`). ArgoCD will reconcile on next sync. |
| 2026-06-16 | F8.3 accepted (ml-router already implemented; model registry noted as temp — will be replaced by DB query after F1.8). F3.1 done: `packages/jwt` (signJwt/verifyJwt, JwtPurpose, payload types) + `services/device-gateway` skeleton (Express, /health, device.auth.middleware, exception middleware). Both build and /health verified. Regenerated package-lock.json (stale prisma-client ref removed). |
| 2026-06-16 | OTA debug (prod device stuck at v2.0.94): traced three bugs blocking firmware updates — out-of-bounds vector access in firmware MQTT callback, CI upload step commented out, wrong deviceType in promote workflow. All three captured in F3.8 for fix after F3.4 + digest work. |
| 2026-06-16 | F3.4 in progress: `services/mqtt-service` (port 3005) — generic MQTT↔queue bridge with TopicRouter handler registry. Handlers: device-status → `q.device.state.changed`, device-telemetry → `q.telemetry.arrived`. Consumer: `q.action.dispatch` → MQTT command publish (uses `firmwareVersion?` field added to `ActionDispatchPayload`). Dockerfile + CI (`mqtt-service-ci.yml`, triggers on master+v2.2) + k8s base + staging overlay wired. VSCode launch (port 9232) + tasks added. Pending: npm install + build verification. |
| 2026-06-16 | F6.1 in progress: `services/digest-service` (port 3006) — authoritative DB writer + Valkey cache + socket emitter, no MQTT. Consumers: `q.telemetry.arrived` (resolve userDeviceActionId via Valkey `action_resolve:` cache 5min + DB fallback; unresolved → throw → DLQ; write `current_state` + `sensor_history` + `action_state:` cache + socket + fan-out `q.rules.evaluate`), `q.device.state.changed` (UserDevice.online + `device_online:` 90s TTL + socket), `q.ota.incoming` (validate semver → audit → forward `q.ota.dispatch` to mqtt-service). OTA chain: `q.ota.incoming → digest → q.ota.dispatch → mqtt-service`. Added `OTA_INCOMING` RK+queue+`OtaIncomingPayload` to packages/queue. Socket via `@socket.io/redis-emitter` (same Valkey pub/sub as Socket.IO adapter). Dockerfile + CI (`digest-service-ci.yml`) + k8s base + staging overlay + Kargo warehouse/stages + VSCode (port 9233) wired. |
| 2026-06-16 | F1.8 (partial) + F6.1 image handling. Created shared `packages/prisma-client` (F1.8 package+generate portion, against current schema): exports `db` (PrismaClient + `@prisma/adapter-pg`) + re-exports generated types; client generated from root schema by transient gitignored `prepare-prisma.mjs`; added to `build:libs`. digest-service rewired to `@lattice/prisma-client` (removed its local prisma shim/deps + Dockerfile builds the shared pkg). Telemetry consumer now branches on action **kind**: resolution joins `device_actions.implementation_type`, classifies image (TakePicture/LiveStream[Http]Action) vs scalar, caches `{id}:{kind}`. **Image** → all frames persisted to `sensor_history` (value widened VarChar(255)→TEXT, migration `20260616000000_widen_sensor_history_value`) + `camera_frame:{deviceId}` cache + `camera_frame` socket; pipeline trigger deferred to F8 (Pipeline model not yet in schema). Added F6.5: historic telemetry/image retention+cleanup. |
| 2026-06-16 | **Backend teardown started** (new "Backend Teardown Tracker" section added). First strip: device-facing routes → `device-gateway` (F3.2/F3.3/F3.6, all `[~]`). Ported from backend: provisioning (`/api/provisioning/provision-token\|provision\|refresh-token`, JWT claim shapes preserved byte-identical for EMQX; refresh now carries `version`), device config (`/api/device/:version/configuration`, device_usage or app_usage+ownership), camera (`/api/camera/frame` rewired to publish `q.telemetry.arrived` → digest handles as image). device-gateway gained `@lattice/prisma-client` + `@lattice/queue`, `app_usage` in its jwt map. Infra: `Dockerfile`, `device-gateway-ci.yml`, `k8s/base/device-gateway`, staging overlay + **ingress path-routing** (`/api/provisioning\|device\|camera` → device-gateway via Traefik longest-prefix; rest → web-app), kargo warehouse+stages. F3.8 infra portion done; 3 firmware OTA bugs still pending. Strangler: monolith code untouched, retired at F14. Build + staging kustomize verified green. |
| 2026-06-17 | **F2.7 Socket.IO server — shipped standalone as `services/socket-server` (port 3007).** Closes the gap where digest-service emits room events (`@socket.io/redis-emitter`) with no server holding the matching adapter. New service: `@socket.io/redis-adapter` over Valkey, `app_usage` JWT handshake (`verifyJwt`, never trusts client userId), `user_{userId}` rooms, inbound `action_state_update` → `ActionDispatchPayload` on `q.action.dispatch` (mqtt-service consumes); no DB. Built on the `v2.2-snapshot` reference but adapted to current `@lattice/jwt` (`AppTokenPayload.userId: string`) + static `@lattice/queue` RKs. Infra mirrors digest: Dockerfile, `socket-server-ci.yml` (master+v2.2, `lattice-socket-server`), `k8s/base/socket-server` + staging overlay, **ingress `/socket.io` moved web-app → socket-server**, VSCode launch/task (inspect 9235, PORT 3007) + `.code-workspace` folder. **Valkey ACL fix** (also unblocks digest's emitter, never exercised until now): added `allchannels` to the user ACL in `compose.yaml` + `k8s/base/valkey` — pub/sub was `NOPERM No permissions to access a channel`; used `allchannels` not `&*` because `&` is a shell control char that silently truncated the command. **Command-state ownership left with digest** (user decision): instead of socket-server echoing optimistically (premature, and would need DB), added a 2nd static queue `q.action.dispatch.state` bound to the same `action.dispatch` RK → fan-out to BOTH mqtt-service (device) and a new digest `action-dispatch.consumer` that writes `current_state` + cache then emits the echo. Extracted digest's action resolution to shared `resolve.ts` + `asString` to `util.ts`. Verified locally end-to-end: `/health` 200, bad-token rejected, valid-token connects, emitter→adapter `user_{id}` room delivery, inbound publish fans out to both queues, **no premature echo**; full staging kustomize renders (43 objects). F2.7 → `[R]`. |
| 2026-06-17 | **F3.8 OTA pipeline fixed + secured + ota-manager workspace-ified.** (1) Firmware crash fix: `MqttActionsHandlerService.h` handles the `ota/...` topic before any `parts[1/3/5/6]` access (+ empty/`<7` guards). (2) Real "never releases" root cause was the **gitops ota-manager init container** overriding the firmware image's `entrypoint.sh` with a dumb `cp` — removed the `command:` override so entrypoint.sh writes `<version>.bin`+`latest.json`; set public `OTA_BASE_URL` per overlay. Firmware distribution is now fully **Kargo/image-driven**; deleted the dead commented `/release` CI block and the whole `esp32-ota-promote.yml` (prod = Kargo `firmware-prod`). (3) **Security:** ota-manager is now **read-only** — dropped the `/release` upload endpoint entirely (firmware publishing is Kargo/image + init-container only, so a second authenticated write path was pure attack surface; anything it wrote got overwritten on the next pod restart anyway). Removed the release-token apparatus (`OTA_RELEASE_TOKEN`, `ota-release-secret` seal entry/docs, `multer`). Remaining auth: device `device_usage` JWT on `/download`+`/check` + rate-limit all endpoints (`trust proxy` for Traefik); firmware attaches its JWT to the OTA download via the HTTPUpdate request callback (no `setAuthorization` in this core); `jwt-secret` reused. (4) **ota-manager moved `ota-manager/` → `services/ota-manager/`** (npm workspace member via `services/*`), uses `@lattice/logger`, monorepo multi-arch Dockerfile (build context → repo root), `ota-manager-ci.yml`/`compose.yaml`/`.code-workspace` updated, VSCode launch+tasks (inspect 9234, PORT 3001). (5) **ota-manager no longer touches MQTT** (user catch — RabbitMQ is the bus): dropped the `mqtt` client/EMQX account/mqtt-certs; on startup it publishes each loaded `latest.json` to `q.ota.incoming` via `@lattice/queue` → digest validates/audits → `q.ota.dispatch` → mqtt-service publishes the retained `ota/updates/<deviceType>`. This finally gives the previously-producerless OTA queue chain its producer. digest `SEMVER` loosened to accept the `v`-prefixed firmware tags; queue package added to ota-manager deps + Dockerfile build. Verified: `pio run -e esp32s3_mini-test` + `esp32s3_cam-test` SUCCESS; digest + queue + ota-manager build; both kustomize overlays render; logger/queue/deps resolve + `node --check` green. **Pending (manual):** deploy + on-device OTA test; enable prod `/download` auth only after fleet is on token-aware firmware. |
