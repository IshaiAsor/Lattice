// Shared Prisma client (F1.8). The api service is the authoritative reader/writer for
// user-facing data (users, catalog, devices, actions, groups).
export { db, Prisma } from '@lattice/prisma-client';
